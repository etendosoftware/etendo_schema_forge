# User

## Intent
This window should let administrators maintain a person’s application identity, review basic security state, define default working context, and assign the roles that control what the user can do in the system.

## What this window should allow
Users should be able to:
- create, find, open, update, and delete user records;
- maintain identity fields such as Name, Email, Description, Position, and Phone (First Name/Last Name were discarded from this window in ETP-4906 wave 12 — see "Reactive behavior and dependencies" below); Email becomes read-only once the record is saved (ETP-4830 — see "Invite-on-create flow" below);
- connect the user to a Business Partner and Supervisor through search-based selectors;
- set a Password only on an EXISTING user (ETP-4830 — the Password input no longer appears on the create form at all, since admin-typed passwords no longer bypass the invite-email flow; it reappears once the record is saved, for resetting an existing user's password) while reviewing read-only security indicators such as Expired Password, Locked, and Last Password Update;
- choose default context values for Language, Client, Organization, and Warehouse;
- assign 1+ system-level template roles (Finance/Sales/Purchasing/Inventory) to an existing user via a multi-select chip control in the header, see a live per-role permission-preview matrix in a dedicated "Roles del usuario" tab before saving, and read/filter each user's applied roles as chips from the Users list grid (ETP-4906, superseding the single-role picker from ETP-4512).

**Role assignment — multi-role composition (ETP-4906).** `AssignTemplateRolesControl` (`tools/app-shell/src/windows/custom/user/AssignTemplateRolesControl.jsx`, wired as `window.headerExtra.customForm`) replaces the old single-select `AssignRoleControl`. It renders the user's currently-selected template roles as removable chips (+N overflow when collapsed) with a checkbox list to add/remove roles, sourced from `fetchTemplateRoles()` (`SFSystemRoleTemplates`) — the 4 fixed system-level template roles (no `isClientAdmin` filter needed: that endpoint never returns a client-admin row at all, there being no such role at system level). **DEV wave 7 repointed this from the original `SFRolesOverview`-backed catalog** — `fetchRolesOverview()`/`SFRolesOverview` is still used elsewhere on this window (the grid's `activeWindowIds` window-exposure filter and `RoleChipsCell`'s admin-detection branch, see below), but no longer supplies the role/column catalog itself. Selection is **local-only until Guardar** — toggling a chip never calls `onChange('defaultRole', ...)` or fires any network request; it only mutates a shared selection held in React Context (`roleSelectionContext.js`, `useRoleSelection()`), consumed by the sibling "Roles del usuario" tab below for its live preview. `defaultRole` itself stays a read-only field on the detail form (unaffected by this control), and the control renders a save-first placeholder for a not-yet-persisted user (composition requires an `AD_User_ID` — never attempted on create). On Guardar, `windows/custom/user/index.jsx` (the window's custom wrapper, registered as `customLoaders['user']` in `tools/app-shell/src/windows/registry.js`) diffs the local selection against the set it fetched on load (`fetchUserRoleAssignments(userId)`, `tools/app-shell/src/lib/userRoleAssignmentsApi.js`) and, only if it actually changed, calls `saveUserRoleAssignments(userId, templateRoleIds)` — a thin wrapper around the existing `SFAssignUserRoles` webhook, which fires exactly once with the FULL desired set (a set-reconciliation call, not additive). On the backend, `SFAssignUserRoles`/`UserRoleCompositionService` (`com.etendoerp.go`) resolve or create the user's personal composition role and reconcile its `AD_Role_Inheritance` rows against the desired template ids — the same most-permissive-wins composition model ETP-4852 already built, unrelated to the legacy `AD_User_Roles`/`Default_Ad_Role_ID` single-row sync ETP-4512 introduced.

**Company user invitations, standalone endpoint (ETP-4894, superseded as the window's primary flow by ETP-4830 below):** `com.etendoerp.go` still exposes an email-only `company-invitation` webhook (`POST /sws/go/company-invitations`) and its own public acceptance page (`InviteAcceptancePage`, `/invite?token=...`), for inviting an *existing* `AD_User` (with roles already assigned) that has no Go account yet. The frontend modal that drove it, `InviteUserDialog.jsx` (`tools/app-shell/src/windows/custom/user/InviteUserDialog.jsx`), is **no longer wired into this window** (ETP-4830 disconnected it from `index.jsx` — see below) but is deliberately kept intact, unwired, as a distinct future entry point for that one case; it is not currently reachable from any menu or button in the app.
- If the invitee already has an active Etendo Go account, they accept the invitation in 1 click (`POST /sws/go/company-invitations/accept`), linking that account to the prepared `AD_User`.
- If the invitee is new, they fill in their display name and set a strong password (`POST /sws/go/company-invitations/register-and-accept`), creating an `Account` locked to the invitation email and linking it to the prepared `AD_User` without changing its roles.
- All invitation lifecycle events (`PENDING`, `SENT`, `ACCEPTED`, `EXPIRED`, `REVOKED`) are recorded in `ETGO_INVITATION` using SHA-256 hashed bearer tokens. Password resets are not repurposed as pseudo-invitations.
- An authenticated Etendo Go account can retrieve its own invitations through `GET /sws/go/company-invitations/mine`. The server derives the account email from the bearer session, so invitations from multiple companies are included while invitations addressed to other accounts remain excluded.

**Invite-on-create flow, the window's primary invitation path (ETP-4830).** Creating a NEW user from this window no longer offers an admin-typed-password bypass — `com.etendoerp.go` always sends the invite email on creation, and the create form reflects that:
- The Password field (`displayLogicJs: "!record.id"` in `decisions.json`) is hidden entirely on the create form and reappears once the record is saved, for resetting an existing user's password — a separate, still-valid use of the same native field, unrelated to invitation.
- Email (`readOnlyLogicJs: "!!record.id"`) becomes read-only once the record is persisted — it seeds `username` and the linked `etgo_account` row at creation time only, so editing it afterward would desync the two (same "locks after first save" pattern as `sales-invoice`/`purchase-invoice`'s `transactionDocument`, see `docs/decisions-reference.md`).
- A pending-invitation status pill ("Invitación pendiente", amber) renders in the detail toolbar's Cancel-button row (`PendingInvitationPill`, wired via `UserPage`'s generic `topbarExtra` slot — not a `decisions.json` extension point, since the field is a 3-state enum `extraBadges`/`statusPills` cannot express) whenever the record's `invitationStatus` field reads `"PENDING"`; it disappears once the backend flips that field away from `PENDING` (e.g. on acceptance). **Stub note:** `invitationStatus`'s exact field name/shape (`"PENDING" | "SENT" | "ACCEPTED" | null`) was the planned backend contract at hand-off time — verify against the real `com.etendoerp.go` contract once that side of ETP-4830 lands.
- On successfully creating a new user, a single actionable toast reads "Usuario creado. Invitación enviada por correo." with a "Configurar roles" action (`handleAfterCreate`, wired via `UserPage`'s `onAfterCreate`) — clicking it re-navigates to the saved record with `location.state.openSecondaryTab: 'custom:roles'` (the same tab-switch mechanism `DetailView.jsx` already uses internally) to open the "Roles del usuario" tab, and scrolls/focuses `AssignTemplateRolesControl` (a separate surface — the `formFooter`, not inside any tab panel). `toast.dismiss()` runs first to clear the generic "record created" toast `useEntity.js` already shows, so only one toast is ever visible.

## Interaction model
- Route: `/user` for the list and `/user/:recordId` for the record detail.
- Visibility: visible in System > Settings from `tools/app-shell/src/menu.json`; it is not marked hidden.
- Implementation type: generated window loaded from `tools/app-shell/src/windows/registry.js` into the shared app-shell list/detail flow.
- Window shape: master-child in the contract (`user` + `emailConfiguration`), and `emailConfiguration` is mounted as the detail child on the detail page (`detailEntity="emailConfiguration"`, `DetailTable={EmailConfigurationTable}`, `DetailForm={EmailConfigurationForm}`). The native `userRoles` child (`AD_User_Roles`, ETP-4512) was `exclude: true`d in `decisions.json` (ETP-4906 manual-QA fix, wave 6) — it used to render as its own read-only secondary tab sharing the exact same translated label ("Roles del usuario") as ETP-4906's new custom tab below, leaking the internal "Personal – &lt;user&gt;" composition role to the admin. It no longer renders at all; the orphaned `UserRolesTable.jsx`/`UserRolesForm.jsx` generated output was deleted (ETP-4906 REVIEW cleanup) once a clean `make regen ONLY=user` was confirmed to no longer emit them.
- List behavior: the generated page exposes list filtering by `name` and `email` (`username` removed from filters and the grid — ETP-4829). The grid also renders a role-chips column (`RoleChipsCell`, `window.customComponents.headerTable: "UserHeaderTable"`) and a toolbar role filter (`RoleFilterControl`) — see "Role assignment — multi-role composition (ETP-4906)" above.
- Detail behavior: the detail page renders the main user form, the multi-role chip control (`AssignTemplateRolesControl`, headerExtra), and a "Roles del usuario" custom tab (`window.customPanelTabs`, live permission-preview matrix, existing-user only) — the only "Roles del usuario" surface on this window since the native `userRoles` tab was excluded (see Window shape above). The toolbar row (Cancel button + status area) also renders a pending-invitation pill when applicable (ETP-4830, `topbarExtra` — see "Invite-on-create flow" above).
- An **Attachments** tab is available in the detail tab strip, allowing files to be attached to the current record.
- **Tab strip order (ETP-4906, DEV wave 10):** Roles del usuario, Configuración del correo electrónico, Adjuntos — in that fixed order. `windows/custom/user/index.jsx` sets `tabOrder: 0` on the `roles` custom tab and passes `detailTabOrder={1}` through to the generated `UserPage`/`DetailView`. Both are required: "Configuración del correo electrónico" is not a plain `secondaryTabs` entry — the generated page renders it via `DetailTable={EmailConfigurationTable}`, which `DetailView.jsx`'s `buildInitialTabs` (`detailViewHelpers.jsx`) treats as the special LINES-tab slot, defaulting to weight `LINES_DEFAULT_WEIGHT = -1` when no `detailTabOrder`/`detailTabIndex` is set — lower than `roles`' own `tabOrder: 0`, which is what made an earlier fix that only touched `tabOrder` silently ineffective. `attachments` is left at its implicit default weight (999) so it keeps sorting last.

**Save-lifecycle hook (`onAfterExistingSave`, ETP-4906).** `AssignTemplateRolesControl`'s selection is local-only until the user clicks Guardar — the shared `DetailView.jsx` component (`tools/app-shell/src/components/contract-ui/DetailView.jsx`) now supports an `onAfterExistingSave(saved, {token, apiBaseUrl})` prop, invoked exactly once per save click, only when `!isNew` (i.e. never before an `AD_User_ID` exists). It mirrors the existing `onAfterCreate` prop's call shape but fires on the *existing-record* save path instead of the create path. This is a **generic, reusable extension point on the shared component** — not a `user`-window-specific hack — any other custom window can pass the same prop to run a side-effecting call after an existing record is saved, exactly the way `windows/custom/warehouse/index.jsx`'s `onAfterCreate` wiring is the precedent this pattern was copied from. Today `user` is the only window that uses it (wired in `windows/custom/user/index.jsx`, which also passes `additionalDirtyState` — a pre-existing `DetailView.jsx` prop, not new — so a role-only chip change enables Guardar even though it never touches a form field).


## Reactive behavior and dependencies
- **First Name/Last Name were discarded from this window (ETP-4906, DEV wave 12, commit `404a0ce70`).** Both fields duplicated the mandatory Name field's Spanish label ("Nombre" for `Name` and for `Firstname` alike, per `es_ES.json`), and no manual-QA test user during this ticket ever used the auto-compose path. `firstName`/`lastName` are now `visibility: "discarded"` in `decisions.json` (removed from both the header form and the list grid), and the `SL_User_Name_Firstname`/`SL_User_Name_Lastname` rule decisions were flipped from `"Keep"` to `"Omit"` to match, alongside the pre-existing `SL_User_Name_Name` `"Omit"` decision — there is no longer a Name/First Name/Last Name synchronization callout on this window at all; Name is now typed directly with no auto-compose behavior.
- Business Partner and Supervisor are search selectors, so they depend on lookup results rather than free-text entry.
- Default Role's read-only *value* remains the parent context for two dependent defaults: Default Client and Default Organization both depend on it, unaffected by ETP-4512/ETP-4906 (neither the legacy `AssignRoleControl` nor its ETP-4906 replacement `AssignTemplateRolesControl` writes `defaultRole` anymore — the field is now purely a display value on this window).
- Default Warehouse depends on Default Client, so warehouse availability should narrow after the client is chosen.
- The native User Roles child (ETP-4512) is excluded from this window entirely as of ETP-4906 (see Window shape above) — `Default_Ad_Role_ID`'s single-row sync (`UserRoleAssignmentHandler`) is unaffected server-side, there is simply no UI surface for it anymore.
- The "Roles del usuario" tab (`UserRolesTab`, ETP-4906) reacts live to `AssignTemplateRolesControl`'s chip toggles with **zero extra network calls per toggle** — it fetches its data once on mount via `Promise.all([fetchMenuTree(), fetchRolesOverview(), fetchTemplateRoles()])` (`SFListMenu`'s menu tree, `SFRolesOverview`, and `SFSystemRoleTemplates`), then reads the shared, not-yet-saved role selection from `roleSelectionContext.js`'s `useRoleSelection()` and recomputes the matrix purely from already-fetched data. **Two role sources, two different jobs (DEV wave 7):** the matrix's own columns (which roles, and their per-window tier data) come from `fetchTemplateRoles()` (`SFSystemRoleTemplates`) — the 4 fixed system-level templates; `fetchRolesOverview()` (`SFRolesOverview`) is kept only for the `activeWindowIds` filter below, since it needs the union across ALL of the calling tenant's roles including its client-admin row (a few real windows are deliberately granted to none of the 4 templates but must still appear as `—` rows), which `SFSystemRoleTemplates`'s response can't supply on its own. It renders one column per currently-selected non-admin template role and one row per menu window (grouped by category, in menu order), filtered down to only windows Etendo GO actually exposes — the union of every `windows[].id` across ALL of `SFRolesOverview`'s roles (Admin included), which drops classic-only AD menu entries (e.g. Application Dictionary) that `SFListMenu`'s tree still returns but Etendo GO never surfaces; any category left with zero surviving rows is omitted entirely (ETP-4906 manual-QA fix, wave 6) — plus 3 hardcoded "General" rows (`Inicio (Dashboard)`, `Favoritos`, `Copilot (Asistente IA)`) rendered as always-✓ for every column since they have no `AD_Window_ID` to derive access from; 9 other windowless rows documented in `com.etendoerp.go`'s `TemplateRoleWindowAccess` javadoc are intentionally omitted entirely (never rendered, not even as a `—` row). Cell values: `✓` (full access), `Solo lectura` (read-only), `—` (absent from that role's `windows[]`). Shows an empty state when zero roles are currently selected. Rendered only for an existing user — `onVisibilityChange(!isNew)` hides the tab entirely while creating a new user.

  **Visual convention (ETP-4906, DEV wave 11, commit `8fe4753d8`).** `✓`/`Solo lectura` cells render as colored rounded pills, not bare text — full access uses the theme's `status-success` semantic tokens (`--status-success-*` CSS custom properties, dark-mode aware), read-only uses `status-warning`; `—` (no access) stays plain text with no pill, same convention `getStatusBadgeProps()`/`FiscalStatusBadge.jsx` already use elsewhere in this app. Category headers ("General" and each `tMenu(group.category)` group, e.g. "Comercial"/"Ventas") render in normal mixed case at `font-medium` weight, not uppercase/bold. Each role column header shows a small `lucide-react` icon before the role name: `TrendingUp` (Sales), `Package` (Inventory), `Landmark` (Finance), `FileText` (Purchasing) — keyed by the same literal role names `roleNameI18n.js`'s `ROLE_NAME_I18N_KEYS` already uses.
- Security state is visible but not user-driven in the current form: Expired Password, Locked, and Last Password Update are read-only fields or summary values.
- No status-driven actions are visible in current evidence. `statusField` is `null`, `extraBadges` is empty, and the page-level `processes` array is empty in `UserPage.jsx`.
- No totals, discount, tax, or document-style recalculation behavior is visible in current evidence for this window.

## Gap assessment
- **Open (ETP-4830 hand-off gap):** `invitationStatus`'s exact field name and value shape on the `user` record (planned as `"PENDING" | "SENT" | "ACCEPTED" | null`) were stubbed against developer-1's `com.etendoerp.go` work in flight at the time this frontend half landed — re-verify `PendingInvitationPill` (`tools/app-shell/src/windows/custom/user/index.jsx`) against the real backend contract once that side is confirmed, and adjust the field key / matched value if it differs.
- `UserPage.jsx` mounts `emailConfiguration` as its detail child entity (`detailEntity="emailConfiguration"`, `DetailTable={EmailConfigurationTable}`, `DetailForm={EmailConfigurationForm}`) — the native `userRoles` entity no longer exists in the contract at all (`exclude: true`d since DEV wave 6), so it is not a candidate detail slot; `emailConfiguration` became the page's only detail entity as a result. This is no longer a gap — see Manual verification step 7.
- The contract's `smtpconnectiontest` action for `emailConfiguration` is exposed: `UserPage.jsx` declares a `detailProcesses` array containing `smtpconnectiontest` ("Test SMTP Connection"), wired to the email-configuration detail pane described above.
- The contract includes `processNow` and `grantPortalAccess` actions on the parent user entity, but the generated detail page exposes no visible process buttons. This is an open gap between contract capability and current page behavior.
- The shared shell supports defaults loading for new records, but there is no user-window-specific evidence here showing what user defaults are prefilled by the backend for a new record.
- ~~The callout-backed synchronization between Name, First Name, and Last Name~~ — no longer applicable: First Name/Last Name were discarded from this window and their callouts flipped to `"Omit"` in ETP-4906 wave 12 (see "Reactive behavior and dependencies" above). This gap is closed by removal, not by verification.

## Manual verification
1. Open `/user` and confirm the list can locate records by `name` and `email`, and that no `username` column or filter is visible.
1a. Create a new user from `/user` with a valid email. Confirm the Password field does not appear anywhere on the create form (ETP-4830 — the admin-typed-password bypass no longer exists) and that a single toast reads "Usuario creado. Invitación enviada por correo." with a "Configurar roles" action; clicking it lands on the "Roles del usuario" tab with `AssignTemplateRolesControl` scrolled into view/focused. Confirm the AD_User's `username` is the lower-cased email for the first client, or the shared client-suffixed form when that email already owns another client; a matching `etgo_account` row is pending, and the invitee receives one password-setup email. Confirm its one-time link activates the account after a strong password is set. A duplicate create must not replace an existing reset token; a missing public app URL or rejected email delivery must leave no new token. Attempt to create a user with a blank email and confirm the request is rejected (400).
1b. Open the newly-created record and confirm the Password field is now visible again (existing-user password reset, unaffected by ETP-4830) and Email is read-only (ETP-4830, locks after first save). Confirm the "Invitación pendiente" pill renders in the toolbar row next to the Cancel button while `invitationStatus` reads `"PENDING"`, and disappears once the invitee accepts.
2. Open `/user/:recordId` and confirm the form shows identity fields, Business Partner and Supervisor lookup fields, security indicators, and default selectors.
3. On an existing user, open the header's role chip control (`AssignTemplateRolesControl`) and toggle 2+ template roles (Finance/Sales/Purchasing/Inventory) on; confirm chips appear/collapse with "+N" overflow and Guardar becomes enabled even though no other field changed. Open the "Roles del usuario" tab and confirm one column appears per currently-toggled role, instantly, with the correct General rows (`Inicio (Dashboard)`, `Favoritos`, `Copilot (Asistente IA)` always ✓) and category-grouped window rows.
4. Click Guardar; confirm it calls the role-assignment save exactly once, Guardar disables again afterward, and reloading the record shows the same roles still selected (persisted). Confirm the row for this user in the `/user` list shows the same roles as chips (`RoleChipsCell`, translated labels) with "+N" overflow when there are more than 2.
5. Toggle a role off, save, and confirm the matrix tab drops that role's column and the grid chip for that user updates accordingly (never leaves a stale/duplicate entry).
5a. Use the Users list toolbar's role filter (`RoleFilterControl`) to filter by a template role, then by "Administrador"; confirm the grid narrows to matching rows in both cases (a classic-Admin user's `defaultRole` counts as a match for the Admin filter even though it has no composed template roles).
5b. Attempt to open the role chip control / "Roles del usuario" tab while creating a brand-new user; confirm the control shows a save-first placeholder and the tab is hidden entirely until the user is saved once.
6. Confirm Expired Password, Locked, and Last Password Update are displayed as review state rather than normal editable business fields.
7. Confirm the current page surfaces an Email Configuration child pane (`DetailTable={EmailConfigurationTable}`/`DetailForm={EmailConfigurationForm}`) with a "Test SMTP Connection" action available from it. Confirm it does NOT surface a Process Now action or a Grant Portal Access action (both remain unexposed gaps — see Gap assessment).
8. Open a saved record and confirm the **Attachments** tab is visible in the tab strip. Upload a file and verify it appears in the table. Download it and delete it. When multiple files exist, confirm 'Download all (ZIP)' and 'Delete all' appear in the table header and that 'Delete all' shows a confirmation dialog before removing all files.

## Automated evidence
- Route visibility is grounded in `tools/app-shell/src/menu.json` and `tools/app-shell/src/windows/registry.js`.
- Parent/child wiring, visible summary state, empty process list, and mounted child entity are grounded in `artifacts/user/generated/web/user/UserPage.jsx`.
- Form sections, read-only security fields, search selectors, and dependent default selectors are grounded in `artifacts/user/generated/web/user/UserForm.jsx`.
- The multi-role chip control (ETP-4906) is grounded in `tools/app-shell/src/windows/custom/user/AssignTemplateRolesControl.jsx`, covered by `tools/app-shell/src/windows/custom/user/__tests__/AssignTemplateRolesControl.vitest.jsx`; the "Roles del usuario" live matrix tab is grounded in `UserRolesTab.jsx`/`__tests__/UserRolesTab.vitest.jsx`; the shared selection channel between them is grounded in `roleSelectionContext.js`/`__tests__/roleSelectionContext.vitest.jsx`; the save-lifecycle wiring (`onAfterExistingSave`, `additionalDirtyState`, `fetchUserRoleAssignments`/`saveUserRoleAssignments`) is grounded in `windows/custom/user/index.jsx`/`__tests__/index.vitest.jsx` and `tools/app-shell/src/lib/userRoleAssignmentsApi.js`/`__tests__/userRoleAssignmentsApi.vitest.js`. The grid's role chips column and toolbar filter are grounded in `RoleChipsCell.jsx`/`__tests__/RoleChipsCell.vitest.jsx`, `RoleFilterControl.jsx`/`__tests__/RoleFilterControl.vitest.jsx`, and `UserHeaderTable.jsx`/`__tests__/UserHeaderTable.vitest.jsx`. End-to-end coverage for the whole multi-role assign/save/reload flow plus the grid role filter is `e2e/tests/flows/user-role-assignment.mocked.spec.js` (see `docs/e2e-testing-guide.md`); the older `e2e/tests/flows/role-assignment.mocked.spec.js` (ETP-4512, single-select control) was deleted as fully superseded once `AssignRoleControl.jsx` was removed.
- The generic `onAfterExistingSave` prop on `DetailView.jsx` (ETP-4906) — invoked once per save, only for an existing record, mirroring `onAfterCreate`'s call shape — is grounded in `tools/app-shell/src/components/contract-ui/DetailView.jsx` and its own Vitest coverage under `tools/app-shell/src/components/contract-ui/__tests__/DetailView.saveActions.vitest.js`.
- On the backend, the multi-role composition read/write path (`SFUserRoleAssignments`, `SFAssignUserRoles`, `UserRoleCompositionService#getAppliedTemplateRoleIds(s)`) is unit-tested in `com.etendoerp.go`'s `SFUserRoleAssignmentsTest`/`UserRoleCompositionServiceTest` — see `docs/neo-headless.md` §8e for the endpoint's request/response contract.
- The `AD_User_Roles` sync from `Default_Ad_Role_ID` is implemented and unit-tested in `com.etendoerp.go`'s `UserRoleAssignmentHandler`/`UserRoleAssignmentHandlerTest` (ETP-4512, untouched by ETP-4906) — see `docs/neo-headless-extensibility.md` §4 "Post-hook: Sync a Related Entity from a Parent Field" (this repo) for the general pattern.
- The `username = email` enforcement and pending `etgo_account` provisioning on create (ETP-4829) are implemented and unit-tested in the same `UserRoleAssignmentHandler`/`UserRoleAssignmentHandlerTest`, plus `EtendoGoAccountProvisioning`/`EtendoGoAccountProvisioningTest` and `EtendoGoJwtDalHelper#createPendingAccount`/`EtendoGoJwtDalHelperTest` (all `com.etendoerp.go`).
- The standalone company-invitation webhook and its dialog (ETP-4894) — `InviteUserDialog.jsx`, covered by `tools/app-shell/src/windows/custom/user/__tests__/InviteUserDialog.vitest.jsx` — is no longer wired into `windows/custom/user/index.jsx` as of ETP-4830 (kept intact, unwired, for a distinct future case — see "Invite-on-create flow" above); the public acceptance page is `tools/app-shell/src/pages/InviteAcceptancePage.jsx`, covered by `tools/app-shell/src/pages/__tests__/InviteAcceptancePage.vitest.jsx`. Both call the `com.etendoerp.go` `CompanyInvitationService` endpoints (`POST /sws/go/company-invitations`, `GET /sws/go/company-invitations/resolve`, `POST /sws/go/company-invitations/accept`, `POST /sws/go/company-invitations/register-and-accept`, `GET /sws/go/company-invitations/mine`), persisted in `ETGO_INVITATION` with SHA-256 hashed tokens and lifecycle status (`PENDING`, `SENT`, `ACCEPTED`, `EXPIRED`, `REVOKED`) — see `docs/specs/ETP-4894-company-invitations-handoff.md` for the endpoint contracts. End-to-end coverage is `e2e/tests/flows/user-invitation.mocked.spec.js` and `e2e/tests/flows/user-invitation.email.integration.spec.js` — see `docs/etendo-go-invitation-e2e-learnings.md` for known cross-client/timing gotchas.
- The invite-on-create flow (ETP-4830) — the pending-invitation `topbarExtra` pill (`PendingInvitationPill`), the actionable "user created" toast (`handleAfterCreate`, `onAfterCreate`), and the `newUser`/`newLabel` fix — is grounded in `tools/app-shell/src/windows/custom/user/index.jsx`, covered by `tools/app-shell/src/windows/custom/user/__tests__/index.vitest.jsx`. Email's locks-after-first-save `readOnlyLogicJs` and Password's create-form-only `displayLogicJs` are declared in `artifacts/user/decisions.json` and grounded in the regenerated `artifacts/user/generated/web/user/UserForm.jsx`.
- Contract evidence for callouts, selectors, child entities, and declared actions is grounded in `artifacts/user/contract.json`.
- Email-configuration UI (`artifacts/user/generated/web/user/EmailConfigurationTable.jsx` and `EmailConfigurationForm.jsx`) is mounted on the detail page — see Gap assessment and Manual verification step 7.
- Shared list/detail shell behavior and defaults loading behavior are described in `docs/generated-custom-windows/app-shell-functional-flows.md`.
- Beyond the role-assignment coverage above (ETP-4906) and the Attachments coverage below, no dedicated browser automation or SPA test was found for the rest of this window's visible behavior (identity fields, security indicators, default-context cascades) in the current repository evidence.
- The generated `UserPage.jsx` includes `AttachmentsTab` in its `customTabs` prop, wired to the `AD_User` AD table — overridden by `windows/custom/user/index.jsx`'s own `customTabs` (which adds the `roles` tab and re-declares `attachments` to keep it present, see that file's own comment on why `customTabs` replaces rather than merges).

## Pipeline regeneration — ETP-3908

Regenerated on 2026-05-12 as part of the feature/ETP-3908 epic merge. No functional changes to this window.

- `linesLayout: "classic"` is now written explicitly to `contract.json`; previously the classic layout was the implicit default.
- `requiredHeaderFields` is now emitted in the page component; this window has no required header fields so the array is empty and there is no behavioral change.
- LinesTable template updated in ETP-3908 to include the inline-editable add-row alignment fix. This window uses `linesLayout: "classic"` so the new template branch is dead code here — no behavioral change.

## Pipeline regeneration — ETP-4906

Regenerated as part of ETP-4906 (multi-role user assignment). `artifacts/user/decisions.json`'s `window` block changed: `headerExtra.customForm` swapped from `AssignRoleControl` to `AssignTemplateRolesControl`; a `customPanelTabs` entry (`key: "roles"`, `component: "UserRolesTab"`) and a `customComponents.headerTable` entry (`"UserHeaderTable"`) were added. Both extension points already existed in the generator/pipeline before this ticket (see `docs/decisions-reference.md` → "Custom Panel Tabs" and "Custom Components") — ETP-4906 only exercises them for `user`, it did not add any new `decisions.json` key or generator capability. The only touched file under `artifacts/user/generated/` is `UserPage.jsx`, and every changed line falls inside its `@sf-generated-start`/`@sf-generated-end` markers.

**Follow-up regeneration — DEV wave 12 (commit `404a0ce70`, contract `0.20.0` → `0.21.0`).** `artifacts/user/decisions.json`'s `user` entity changed independently of the above: `firstName`/`lastName` fields set to `visibility: "discarded"`, and the `SL_User_Name_Firstname`/`SL_User_Name_Lastname` rule decisions flipped from `"Keep"` to `"Omit"` (fixing a duplicate "Nombre" label collision in `es_ES.json`, unrelated to role assignment — see "Reactive behavior and dependencies" above). `make regen ONLY=user` re-ran; `npx sf-validate-pipeline --scope=user` confirmed OK. `UserHeaderTable.jsx`'s hand-written `columns` array (custom `headerTable` override, not generated) was manually re-synced to drop the same two columns, per that file's own re-sync comment.

## Pipeline regeneration — ETP-4830

Regenerated as part of ETP-4830 (contract `0.21.1` → `0.22.0`, additive). `artifacts/user/decisions.json`'s `user` entity `email` field gained `readOnlyLogicJs: "!!record.id"` (locks after first save) and its `password` field gained `displayLogicJs: "!record.id"` (hidden on the create form only) — both compile into the generated `UserForm.jsx`'s `readOnlyLogic`/`displayLogic` function properties, confirmed by re-checking that file after `make regen ONLY=user`. Neither `readOnlyLogicJs` nor `displayLogicJs` were previously documented in `docs/decisions-reference.md` despite already existing in the generator/pipeline (used by `sales-invoice`/`purchase-invoice`'s `transactionDocument` and `assets`/`asset-group`'s depreciation fields respectively) — both are now documented there under "Logic & Behavior" as part of this ticket, since neither is a new `decisions.json` key or generator capability, only a newly-filled documentation gap. `npx sf-validate-pipeline --scope=user` confirmed OK. The `window` block (headerExtra/customPanelTabs/customComponents) was untouched — only `contract.json` and `UserForm.jsx` changed under `artifacts/user/generated/`, every changed line inside its `@sf-generated-start`/`@sf-generated-end` markers.

The pending-invitation `topbarExtra` pill, the `onAfterCreate` actionable toast, and the `newLabel` fix are all wired as plain props in `windows/custom/user/index.jsx` (not a `decisions.json` change) — see "Invite-on-create flow (ETP-4830)" above.
