# Return to Vendor Shipment

## Intent
This window should let a purchasing user complete the physical outbound shipment that sends returned goods back to a vendor. It sits after the return-to-vendor authorization step and before any vendor credit-memo settlement, so its business meaning is operational shipment execution rather than commercial approval or invoicing.

## What this window should allow
Users should be able to review or prepare a vendor-return shipment header, confirm who the goods are being returned to, set shipment and accounting dates, choose the warehouse context, and keep notes for the shipment record.

For a draft shipment, the window should also let the user:
- pick or edit the shipment lines that will be sent back to the vendor — **lines are import-only (ETP-4462):** new lines can only be created by importing them from the source goods receipt via the "Añadir desde Albarán" modal; manual line entry is disabled
- review each shipped product, quantity, UOM, storage bin, and traceability back to the originating return source
- process the shipment so the document moves out of draft once the outbound return is ready to complete
- copy a direct link to a record — from the list selection bar when exactly one row is selected, or from the record detail view once the record is saved

Because the line model points back to a return source, this window should primarily support completing a shipment derived from an approved vendor return rather than building an unrelated outbound movement from scratch. Since ETP-4462 the SPA enforces this: return lines must be imported from the source goods receipt (see the import-only lines bullet in the Interaction model below).

## Interaction model
- Route: `/return-to-vendor-shipment`, `/return-to-vendor-shipment/:recordId`
- Visibility: visible in the Purchases menu
- Implementation type: generated window registered in `tools/app-shell/src/windows/registry.js` and loaded through the generic app-shell window loader
- Window shape: master-child window with a header entity on `M_InOut` and child lines on `M_InOutLine`.
- An **Attachments** tab is available in the detail tab strip, allowing files to be attached to the current record.
- Lines tab layout: this window uses `window.linesLayout = "inlineEditable"`. Rows render at 40 px with pencil and trash hover-action icons on the right; clicking pencil flips the row into inline edit; trash removes the row after confirmation. When the add-row form is open, existing rows stay in `InlineLinesPanel` so column widths remain stable; the form renders in a header-hidden `DataTable` below that handles callouts, selectors, and focus. Clicking "Añadir línea" while a form is already open saves the current line and opens a fresh form scrolled into view. See `docs/ui-customization.md` section 13 for the full reference.
- **Send email ("Enviar") action (ETP-4718):** available in both the grid row-hover quick actions and the form/preview action bar, visible only when `documentStatus === 'CO'` (Confirmado). See the ETP-4718 entry in the Gap assessment section below for the full wiring detail and the known Message-field limitation.
- **Import-only lines (ETP-4462):** manual line creation is disabled on this window. `window.maxDetailLines: 0` in `artifacts/return-to-vendor-shipment/decisions.json` makes the generator emit `addLineGuard={(_, children) => children.length < 0}` in the generated `ReturnToVendorShipmentPage.jsx`, so `DetailView` resolves `canAddLines` to `false` unconditionally. Effects: the lines empty state hides the primary "+ Añadir líneas" button and shows only the "Añadir desde Albarán" CTA with the import-only description key `linesImportOnlyFromReceipt` ("Import the lines from the goods receipt to return"); and `DetailView` suppresses the add-line area below the lines table (manual add button and its kebab menu included). To keep importing possible on a draft that already has lines, the bottom panel (`artifacts/return-to-vendor-shipment/custom/ReturnToVendorShipmentBottomPanel.jsx`) re-renders the "Añadir desde Albarán" trigger (`ReturnToVendorLineActions`) itself, above `LinesBottomSection`, when the record has lines (`props.lines.length > 0`), is in draft (`documentStatus === 'DR'`), and has a business partner set. Known limitations of this panel-rendered path: it passes no `onSave`, so the header is not saved before the import modal opens, and it refreshes via a full `window.location.reload()` after a successful import (`DetailView`'s `bottomSection` contract exposes no refresh callback). The same panel's custom empty state forwards the `canAddLine` prop to the shared `LinesEmptyState` and switches the description key on it. Note: the manual "Añadir línea" interactions described in the previous bullet are therefore not reachable on this window — inline row editing (pencil) and row deletion (trash) still work on imported lines. Full pattern reference: `docs/ui-customization.md` section 11 (`window.maxDetailLines`).

At list level, the window exposes return-to-vendor shipments as purchase records. At detail level, it renders the shipment header plus child lines and a related-documents tab. The contract uses the default layout, marks `description` as the notes field, and enables related-documents support.

## Reactive behavior and dependencies
The main visible parent-child dependency is the standard shipment header to shipment lines relationship: child rows belong to the selected header record, and app-shell behavior refreshes child rows and the header together after line changes.

Observed shipment-specific reactions and dependencies include:
- **Draft-only actions:** `Pick/Edit Lines` is only shown while `Processed = N`. `Process Shipment` is shown while the document is not closed or voided.
- **Header editability changes by status:** business partner, partner address, movement date, warehouse, and several accounting dimensions become read-only after processing or posting conditions defined in the contract.
- **Agentic address dependency:** `partnerAddress` is treated as dependent on `businessPartner`, passing `C_BPartner_ID` to the selector so vendor-return shipment agents can resolve valid partner locations without hardcoded IDs.
- **Date defaulting:** movement date and accounting date both default to the current date in the contract.
- **Header-driven callouts:** the contract attaches callouts to business partner, movement date, and warehouse, which suggests dependent updates for address/accounting/warehouse context even though the exact UI result is not documented in current evidence.
- **Return-source traceability on lines:** the line model includes a read-only `Return to Vendor line` reference (`C_OrderLine_ID`), plus product, movement quantity, UOM, storage bin, and optional attribute-set value.
- **Related-document navigation:** the generated detail view exposes a Related Documents tab because the contract marks the window as `relatedDocuments: true`.

Current evidence does not show totals, discounts, taxes, or pricing reactions on this shipment window, which is consistent with a logistics document rather than a commercial order.

Copy-link visibility (ETP-4721): in the grid selection bar, `Copy link` appears only when exactly one row is selected — hidden with 0 or 2+ rows selected. In the detail topbar, `Copy link` is visible whenever the record has a persisted `recordId` (not the unsaved `'new'` sentinel), with no selection gate since detail always represents a single record. Both copy `{origin}/{windowName}/{recordId}` to the clipboard, show a `Link copied` / `Enlace copiado` toast, and display a `Copy link` / `Copiar enlace` tooltip on hover. The legacy dead link icon previously shown in the idle-state (no-selection) grid toolbar is now hidden via the `hideLink` prop passed to `<ListView>`. The detail-topbar button is rendered as a sibling of `ConfirmWithCreditButtonBase` inside `ConfirmWithCreditButton.jsx`, so it stays visible even when the base component itself returns `null` outside DR/CO status.

## Gap assessment
- The business flow strongly suggests this shipment should be created from an approved vendor return or RMA-like source, but the current generated header form does not visibly expose an explicit return header field such as `M_RMA_ID`. The linkage is implied through the document purpose, the research findings, and the line reference back to the return source, but the exact user-facing source-selection flow is not fully evidenced.
- The generated page defines dependent add-line fields for a return order line and an RMA line, but the rendered line form in current evidence is largely read-only and does not expose those same editable dependencies in a clearly supported way. This leaves ambiguity about whether users actually add lines manually in the SPA, rely entirely on `Pick/Edit Lines`, or get lines from another server-driven workflow. **Partially resolved by ETP-4462:** manual line entry in the SPA is now explicitly disabled (`window.maxDetailLines: 0`), so lines come from the import-from-receipt modal (or server-driven flows where exposed) — the manual-add ambiguity no longer exists.
- Shipment-completion semantics imply an outbound stock movement that decreases inventory, but the current SPA evidence does not directly prove the stock effect, reservation changes, or warehouse-on-hand updates. That should be treated as expected ERP behavior, not confirmed UI behavior.
- The document-status field and process button show lifecycle state, but the exact status transition path for this window is not covered by a dedicated browser test.
- Related-document support is clearly enabled, but the exact linked targets shown to users for vendor returns versus receipts or credit memos are not demonstrated in current evidence.
- **ETP-4408 — Confluence DF "Documento A — Albarán de Devolución" (space PYPI, page "Compras"):** the row-preview panel the DF asks for (PDF left panel; right panel with Enviar/Descargar PDF/Editar, General/Mensajes/Historial tabs, Estado section with status badge + billing-status progress bar, Documentos Relacionados) **mostly already existed**, shipped since ETP-4034/ETP-4208 — `windows/custom/return-to-vendor-shipment/ReturnToVendorShipmentPreview.jsx` + `useReturnToVendorPdf.js`, wired via `rowQuickActions.documentPreview: true` + `renderPreview` in this window's `index.jsx`, reusing the same shared building blocks as `goods-shipment` (`GenericPreviewModal`, `PreviewPdfPanel`, `SummaryCard`'s `PercentBar`/`InfoRow`/`CardShell`, `RelatedDocumentsCard`). **Note:** at the time this note was written the "Enviar" button in that right panel was rendered but not functional — it had no `onEmail` handler. See the ETP-4718 bullet below for when it was actually wired.
  The billing-status progress bar was the one piece that was **not** actually wired despite the note above — added under ETP-4408: `invoiceStatus` is now exposed (`readOnly`, `columnType: "percent"`) as a list column (`ReturnToVendorShipmentTable.jsx`) and as an "Invoiced:" row using the same `PercentBar` shared with orders/invoices, rendered in `ReturnDocStatsPanel.jsx` (shared by this window and `return-material-receipt`).
  Other change made under ETP-4408: discarded `etblkpAccountingstatus` (the sole field on this window's "Otros" form tab, also present on `goods-shipment`) — internal accounting-posting state, not part of the DF and not needed here.
  **Bug fixed under ETP-4408 (same defect as `return-material-receipt`, pre-existing since 2026-03):** `businessPartner` (Contacto) stayed editable even on completed shipments, because its AD rule (`@Processed@='Y' | @HAS_M_INOUTLINES@='Y'`) includes a session variable the pipeline can never translate to client-side JS. Fixed the same way — overrode `readOnlyLogic` with the simplified `"@Processed@='Y'"`. Same residual gap as the sales-side window: the "lock once lines exist, even in draft" half of the original rule isn't enforced client-side.
  (`warehouse` and `partnerAddress` were checked too and found not actually broken — see `return-material-receipt.md` for why their `"readOnlyLogic": null` was always a no-op, not a real silencer. Left untouched.)
  **ETP-4737 — "Crear Factura Rectificativa" rename:** mirroring `return-material-receipt.md`'s equivalent note, the invoice this window's confirm action can generate is now the unified "Factura Rectificativa (compras)" doc type (see `purchase-invoice.md`), not the retired `AP CreditMemo`/return-invoice concept. `return-to-vendor-shipment/ConfirmWithCreditButton.jsx` overrides `cardTitle`/`cardDesc` on the shared `ConfirmWithCreditButtonBase.jsx` with the window-scoped `returnToVendor.createCreditNote` / `returnToVendor.createCreditNoteDescription` keys (now reworded to "Create Rectificative Invoice" / a rectificative-invoice description, in all 3 locales — `en_US`, `es_ES`, `es_AR`), and the confirm-modal copy (`returnToVendor.confirmModal.infoRowPost`, `returnToVendor.confirmModal.confirmWithInvoice`) was reworded the same way. Backend: on the confirmed-return confirm flow, `ReturnToVendorShipmentHeaderHandler`'s `createReturnInvoice` action resolves the doc type via `ReturnShipmentUtils.findReturnDocTypeForOrg(orgId, "API", isSales=false, requireReturn=false, requireRectificative=true)` — the unified rectificative type (`documentCategory="API"` + `EM_Etsg_Isrectificative`), replacing the legacy plain-`APC` ("AP CreditMemo") lookup — and the generated invoice carries negative line quantities/totals, pre-existing logic untouched by this ticket. **This wording fix landed as a QA-reported bug during this same epic** (`tools/app-shell/src/windows/custom/return-to-vendor-shipment/__tests__/rectificativa-wording.i18n.vitest.jsx`): an earlier pass renamed the sales-side `return-material-receipt` wording but missed this purchase-side sibling entirely, leaving the old "Nota de Crédito"/"factura de devolución de compra" phrasing in place in all 3 locales; the test now guards against a regression. **Known residual gap (same as `return-material-receipt.md`):** the secondary CO-status pill button (shown when the shipment is already completed and has no return invoice yet) still renders the generic, unrenamed `ui('createReturnInvoice')` label from `ConfirmWithCreditButtonBase.jsx` — that component has no override prop for this specific button today. Left as-is to avoid touching the shared component mid-task.
- **ETP-4718 — Send-email ("Enviar") action actually wired, gated to Confirmado:** the "Enviar" affordance existed as inert UI in both surfaces since ETP-4034/ETP-4208/ETP-4408 (see the ETP-4408 bullet above); this ticket is what makes it functional, in both places, only while `documentStatus === 'CO'` (Confirmado):
  - **Grid row-hover:** `windows/custom/shared/ReturnWindowShell.jsx` now accepts an optional `emailAction = { usePdf, documentType, visibleWhen }` prop, wired from this window's `index.jsx` as `emailAction={{ usePdf: useReturnToVendorPdf, documentType: tMenu('Return to Vendor Shipment'), visibleWhen: "@documentStatus@='CO'" }}`. The shell threads it into `rowQuickActions.email.visibleWhen` and `rowQuickActions.onEmail` via the shared `useRowEmailModal` hook, which opens `SendDocumentModal` with the same client-generated PDF preview the row-preview panel uses. Before this PR the row's email icon was additionally suppressed altogether by an explicit `decisions.json → window.sendDocument: { enabled: false }` override (removed in this PR) — the row-hover email icon simply never rendered, regardless of `documentStatus`.
  - **Form/preview action bar:** `ReturnToVendorShipmentPreview.jsx` now passes `onEmail: isSendable ? sendModal.openEmailModal : undefined` to the preview's action buttons (`isSendable = shipment.documentStatus === 'CO'`), using the shared `usePreviewSendModal` + `ReceiptSendModal` from `windows/custom/shared/PreviewActionButtons.jsx`. `buildReturnPreviewContent.jsx` forwards the optional `onEmail` through to `PreviewActionButtons` so the "Enviar" button only appears when a handler is supplied.
  - **Visibility rule:** hidden in Borrador (`'DR'`) and any non-Confirmado state on both surfaces — there is nothing to email until the document is confirmed.
  - **Backend contract:** unchanged by this PR — `return-to-vendor-shipment-send` (the `${windowName}-send` NEO pseudo-spec convention, see `documentEmailSend.js`) already existed in `com.etendoerp.go`.
  - **Known limitation (platform-wide, not specific to this window, out of scope for ETP-4718):** the "Mensaje" (Message) field in the send dialog (`SendDocumentModal.jsx`'s `EmailFormPanel`) currently renders `readOnly`. This affects every documental window that uses `SendDocumentModal`, not just this one.

## Manual verification
1. Open `/return-to-vendor-shipment/:recordId` for a draft record.
2. Confirm the header shows vendor-facing shipment context: business partner, partner address, movement date, accounting date, warehouse, description, and document status.
3. Select or change a business partner and confirm partner address options are scoped to that selected vendor.
4. Confirm draft records expose both `Pick/Edit Lines` and `Process Shipment`.
5. On a draft shipment with no lines, confirm the empty state shows ONLY the "Añadir desde Albarán" button (no "+ Añadir líneas" primary button) with the import-only description "Import the lines from the goods receipt to return". On a draft shipment WITH lines, confirm the "Añadir desde Albarán" link renders below the lines table while no manual "+ Añadir línea" button or kebab menu appears, and that pencil/trash row editing still works; run an import from that link and note the page fully reloads afterwards. On a completed shipment, confirm the import link is gone.
6. Use `Pick/Edit Lines` and verify the resulting shipment lines reflect the return source, including product, movement quantity, UOM, storage bin, and the `Return to Vendor line` reference when available.
7. Process the shipment and confirm the document moves out of draft and the draft-only actions disappear or become unavailable.
8. If related documents exist, open the Related Documents tab and verify it links the shipment back to the surrounding return flow.
9. If stock-movement behavior is important for the rollout, verify separately in the ERP/backend that processing this shipment produces the expected outbound inventory effect, because that consequence is not directly proven by current SPA evidence.
10. Open a saved record and confirm the **Attachments** tab is visible in the tab strip. Upload a file and verify it appears in the table. Download it and delete it. When multiple files exist, confirm 'Download all (ZIP)' and 'Delete all' appear in the table header and that 'Delete all' shows a confirmation dialog before removing all files.
11. In the list, select 0, then 1, then 2+ shipments and confirm `Copy link` appears in the selection bar only when exactly one row is selected. Click it and confirm a `Link copied` toast appears and the clipboard contains `{origin}/return-to-vendor-shipment/<id>`. Open a saved shipment and confirm the same `Copy link` action (with tooltip on hover) is available in the detail topbar.
12. **ETP-4737:** open a draft return-to-vendor shipment and confirm the confirm-flow card reads **"Create Rectificative Invoice"** (not "Nota de Crédito" or any "factura de devolución" wording) in all 3 locales. Confirm and generate the invoice, then confirm it lands under the purchase-invoice "Facturas rectificativas" tab with negative line quantities/totals. On a completed shipment with no invoice yet, confirm the secondary pill button still reads the generic "Crear Factura de Devolución" label (known residual gap, not fixed here).
13. On the list, hover a row for a Borrador (draft) shipment and confirm the "Enviar" quick action is NOT shown. Hover a row for a Confirmado shipment and confirm "Enviar" IS shown; click it and verify `SendDocumentModal` opens with a PDF preview. Repeat inside the row-preview panel's action bar (open the preview for a Confirmado shipment and confirm the "Enviar" button opens the same send dialog; open it for a Borrador shipment and confirm no "Enviar" button is rendered). In the send dialog, confirm the "Mensaje" field is present but not editable — this is expected, platform-wide behavior, not a defect of this window.

## Automated evidence
- `tools/app-shell/src/menu.json` places `return-to-vendor-shipment` in the Purchases menu.
- `tools/app-shell/src/windows/registry.js` registers the generated window route.
- `artifacts/return-to-vendor-shipment/contract.json` defines a default-layout `M_InOut` header with `relatedDocuments: true`, draft-sensitive shipment actions, status/read-only logic, current-date defaults, and `M_InOutLine` child records.
- `artifacts/return-to-vendor-shipment/generated/web/return-to-vendor-shipment/ReturnShipmentPage.jsx` shows the detail view with child lines and the Related Documents tab.
- `artifacts/return-to-vendor-shipment/decisions.json` sets `window.maxDetailLines: 0`, and the generated `ReturnToVendorShipmentPage.jsx` passes `addLineGuard={(_, children) => children.length < 0}` to `DetailView` — the source-level evidence for the import-only lines behavior (ETP-4462). The import-only empty-state description key `linesImportOnlyFromReceipt` exists in `en_US`, `es_ES`, and `es_AR` locale files. `ReturnToVendorShipmentBottomPanel.jsx` re-renders `ReturnToVendorLineActions` above `LinesBottomSection` when `props.lines.length > 0` on a draft with a business partner, keeping the import trigger available once lines exist (panel-rendered; refreshes via `window.location.reload()`).
- `artifacts/return-to-vendor-shipment/generated/web/return-to-vendor-shipment/HeaderForm.jsx` and `LinesForm.jsx` show the currently surfaced header and line fields.
- `artifacts/return-to-vendor-shipment/FINDINGS.md` documents the intended logistics meaning of this window as vendor outbound movement (`MovementType = V-`) and its relationship to the return-to-vendor authorization flow.
- **ETP-4737** — `tools/app-shell/src/windows/custom/return-to-vendor-shipment/ConfirmWithCreditButton.jsx` proves the window-scoped `returnToVendor.createCreditNote`/`createCreditNoteDescription`/`confirmModal.infoRowPost`/`confirmModal.confirmWithInvoice` label overrides on the shared `ConfirmWithCreditButtonBase.jsx`. `tools/app-shell/src/windows/custom/return-to-vendor-shipment/__tests__/rectificativa-wording.i18n.vitest.jsx` is a regression test (added as a QA-reported-bug fix during this epic) asserting none of the four keys reference the old "Nota de Crédito"/"Credit Note"/"factura de devolución" wording in `en_US`, `es_ES`, or `es_AR`.
- There is no dedicated `return-to-vendor-shipment` browser test in current evidence. Shared route loading and generic entity data behavior are documented in `docs/generated-custom-windows/app-shell-functional-flows.md`.
- The generated `ReturnShipmentPage.jsx` includes `AttachmentsTab` in its `customTabs` prop, wired to the `M_InOut` AD table.
- **ETP-3995 — Related Documents tab i18n**: The generated page file now uses `labelKey: 'relatedDocuments'` in the `customTabs` prop instead of a hardcoded `label: 'Related Documents'` string, so the tab title renders via the active UI language (e.g. "Documentos relacionados" in Spanish) regardless of the browser locale.
- **ETP-4721 — Copy link**: `tools/app-shell/src/hooks/useCopyLinkAction.js` implements `useCopyLinkAction` (grid selection-bar copy) and `useCopyRecordLinkAction` (detail-topbar copy); `tools/app-shell/src/components/contract-ui/CopyLinkButton.jsx` and `CopyRecordLinkButton.jsx` render the tooltip-wrapped buttons for each context. `tools/app-shell/src/windows/custom/return-to-vendor-shipment/index.jsx` wires the grid action into `bulkActions` and passes `hideLink` to `<ListView>`; `tools/app-shell/src/windows/custom/return-to-vendor-shipment/ConfirmWithCreditButton.jsx` (the `topbarRight` component for this window) wires `CopyRecordLinkButton` into the detail topbar, rendered as a sibling of `ConfirmWithCreditButtonBase` so it stays visible even when the base component returns `null`.
- **ETP-4718 — Send-email action wiring:** `windows/custom/return-to-vendor-shipment/index.jsx` passes `emailAction={{ usePdf: useReturnToVendorPdf, documentType: tMenu('Return to Vendor Shipment'), visibleWhen: "@documentStatus@='CO'" }}` to `windows/custom/shared/ReturnWindowShell.jsx`, which threads it into `rowQuickActions.email` / `rowQuickActions.onEmail` via `windows/custom/shared/useRowEmailModal.jsx`. `ReturnToVendorShipmentPreview.jsx` gates `onEmail` on `shipment.documentStatus === 'CO'` and wires `usePreviewSendModal`/`ReceiptSendModal` from `windows/custom/shared/PreviewActionButtons.jsx`; `buildReturnPreviewContent.jsx` forwards the optional `onEmail` to `PreviewActionButtons`. `artifacts/return-to-vendor-shipment/decisions.json` and `contract.json` no longer set `window.sendDocument: { enabled: false }` on the header or lines entities, so the generated `ReturnToVendorShipmentPage.jsx` now passes `sendDocument` (enabled) instead of `sendDocument={{"enabled":false}}` to `DetailView` — removing the explicit override that had suppressed the row-hover email affordance regardless of `documentStatus`. The backend contract this hits, `return-to-vendor-shipment-send`, already existed and was not touched by this PR.

## Theme roles

The window's live artifact custom components use the shared semantic theme.
Structural surfaces and controls consume background, card, foreground, muted, and
border roles; operational feedback uses success, warning, information, neutral,
and destructive roles. No local palette is used, so the active application theme
controls the appearance.
