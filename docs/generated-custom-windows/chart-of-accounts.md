# Chart of Accounts

## Intent
Maintain the account master used by finance users and provide a quick, read-only review of each account's debit, credit, and current balance from the same window.

## What this window should allow
- Browse the account list from the Finance menu.
- Search accounts by code, name, and account type.
- Create subaccounts from the custom tree toolbar using the selected parent account; the generic Create New list action is hidden.
- Open an existing account and update those same setup fields, except protected parent-like subaccounts whose 8-digit code ends in `0000`.
- Delete an account through the standard generated entity flow.
- Review debit, credit, and balance values in the list as accounting outputs, not as manually editable form inputs.
- Filter the tree by code, name, or account type using the toolbar filter row — matches on displayed leaf or virtual-folder code/name (including letter-suffixed codes such as `430A`) retain the matching folder and its relevant descendants, while account-type filtering remains applied to leaves; non-matching branches are hidden.
- Deactivate or reactivate an existing subaccount directly from the tree via an inline toggle (protected `0000`-suffixed placeholder subaccounts cannot be toggled).
- Toggle whether the account is active directly from the tree row; the form's own `isActive` control follows the same live-toggle behavior rather than staying read-only.

## Interaction model
- Route: `/chart-of-accounts` for the list and `/chart-of-accounts/:recordId` for record detail.
- Visibility: visible from the Finance menu as **Chart of Accounts**.
- Breadcrumb: `Finanzas / Plan de cuentas` — driven by `decisions.json → window.category: "finance"`, which the generated `ElementValuePage.jsx` resolves through `tMenu()` alongside the window name. ETP-4945 corrected this from `category: "accounting"`, which rendered the wrong section (`Contabilidad / Plan de cuentas`).
- Implementation type: generated window route loaded from the app-shell window registry.
- Window shape: single-entity window for `elementValue`, with the runtime custom grouped tree table at `artifacts/chart-of-accounts/custom/AccountTreeView.jsx` replacing the generated list table.
- Record detail titles use the account code (`searchKey`) rather than the internal record id.
- The window title carries no record-count badge (`window.hideRecordCount: true` in `decisions.json`). The generic count would be misleading here — `hook.items.length` only ever reflects one paginated batch of leaves (see the "Full-dataset self-fetch" note below), not the full materialized tree the page actually shows.
- The tree renders the FULL Etendo Classic account hierarchy as nested, expandable folders — matching Classic's "Combinación de cuentas" grouped view exactly. For example, account `20000000` nests 6 levels deep: `A` (Heading: ACTIVO) → `A.A` (Heading) → `A.A.I` (Heading) → `200` (Account) → `2000` (Breakdown) → `20000000` (Subaccount). Every folder is collapsed by default; expand/collapse state is persisted to `localStorage` (`sf.chartOfAccounts.expandedFolderIds`) so navigating away to another window and back restores exactly what the user left open.
- The chart-of-accounts toolbar actions (Expand/Collapse and New Sub-account) and the text/type filters share one sticky, opaque controls wrapper anchored to the existing `ScrollPane`. The wrapper has no nested overflow container, so the tree itself remains the only scrolling surface while controls stay visible during long-tree scrolling on desktop and mobile.

## Reactive behavior and dependencies
- There is no visible parent/child interaction because the window only exposes the `account` entity.
- `Parent Account` is the only cross-record dependency in the form. It is rendered as a search-based foreign-key field, so users can link an account to another account, but no catalog preload, hierarchy browser, or auto-filtering behavior is visible in the current generated assets. In the runtime custom subaccount-creation modal (`artifacts/chart-of-accounts/custom/NewAccountModal.jsx`), the parent selector is a searchable combobox (`AccountBadgeSelect`) rather than a plain list, and Account Type now defaults from the selected parent/sibling account's existing type instead of always defaulting to Expense.
- Debit, credit, and balance appear only in the table and are read-only in the contract, so the current surface behaves as account setup plus financial review rather than as a balance-editing screen.
- No dependent selectors, status-driven actions, totals, discounts, tax reactions, or line-level recalculations are visible in the current evidence.
- New subaccount creation is handled by the custom modal. The action is always available; when a branch or account row is selected, the modal defaults the parent selector from that row, and otherwise it opens with no parent selected.
- The 4-digit suffix field in the new-subaccount modal shows the **next available suffix** (highest existing sibling under the selected parent prefix, plus 1, clamped at `9999`) as its placeholder (e.g. `0005` when `20000001..20000004` already exist), computed client-side from the already-loaded account list; falls back to the generic `0000` placeholder when nothing exists yet under the prefix. Purely a hint — does not pre-fill or validate the field.
- Submitting a duplicate 8-digit code (one that already exists for the client) is rejected by `ChartOfAccountsHandler` with a specific `"Account <code> already exists."` message, translated and shown as a toast — not the generic "Could not create sub-account" fallback, which is now reserved for genuinely unexpected save failures.
- Account Type values are rendered from AD list translations extracted from `AD_REF_LIST_TRL`, so raw AD values (`A`, `E`, `L`, `M`, `O`, `R`) display consistently in English and Spanish.
- **Element Level column (ETP-5399):** the tree grid shows a dedicated `Element Level` column (`Heading`/`Account`/`Breakdown`/`Subaccount`), positioned between Name and Account Type, matching Etendo Classic's account list. It reads `item.elementLevel` — the same `C_ElementValue.ElementLevel` value already injected onto every row (real leaf or virtual folder) by `ChartOfAccountsHandler.injectHierarchyFields` for the hierarchy/insertion logic above, now also surfaced to the user. Label mapping lives in `ELEMENT_LEVEL_UI_KEYS`/`elementLevelLabel` (`artifacts/chart-of-accounts/custom/accountTypeLabels.js`), mirroring the pre-existing `ACCOUNT_TYPE_UI_KEYS` pattern. `decisions.json` still marks the raw `elementLevel` entity field as `discarded` — that only governs the generic generated form/contract, not this custom tree render, which renders it directly from the handler-injected row data.
- Parent-like posting subaccounts with an 8-digit code ending in `0000` are protected. The form renders Code, Name, Description, and Account Type as read-only for existing protected records (`readOnlyLogic: "@ProtectedParentLikeSubaccount@='Y'"` in `decisions.json`, backed by `ChartOfAccountsHandler.isProtectedParentLikeSubaccount`), and the backend rejects creating or modifying those codes. These leaves are technically `issummary='N'` (real DB leaves) but still function as placeholders under their breakdown group — the tree view marks them with a lock icon (`account-tree-locked-<id>`) even though they remain clickable for read-only viewing. Real subaccounts (e.g. `20000001`) are unaffected and stay fully editable.
- The tree's full hierarchy comes from a per-leaf `ancestors` array (root-to-leaf, `{value, name, elementLevel}`) injected by `ChartOfAccountsHandler`, built by walking the client's `AD_TreeNode`/`AD_Tree` ("`<ClientName> Element Value`", `ad_table_id = 188`). `elementLevel` mirrors `C_ElementValue.ElementLevel` (`E` Heading, `C` Account, `D` Breakdown, `S` Subaccount). The legacy 2-level `parentCode4`/`parentCode4Name` fields are still injected for backward compatibility with `NewAccountModal`'s parent selector and as a fallback if `ancestors` is ever absent.
- **New sub-account parent/prefix resolution is `ElementLevel`-based, not string-shape-based (ETP-5399).** The original logic derived a new sub-account's parent prefix by treating any 4-character code as "the summary account". That silently mis-resolved letter-suffixed `430A`, which is an Account-level (`C`) node, instead of drilling into its actual Breakdown-level (`D`) children: `4300A`, `4304A`, and `4309A`. The fix keys off `C_ElementValue.ElementLevel` instead of the `Value` string's shape:
  - **Backend:** `ChartOfAccountsHandler`/`ChartOfAccountsTreeMath` (com.etendoerp.go) compute `insertionChildren` per leaf/node — the real, structural insertion candidate(s) for a new subaccount under it — and inject that field into the relevant NEO responses. A Breakdown (`D`) node is always the correct structural parent depth for insertion, regardless of whether its identity is numeric (`2000`) or letter-suffixed (`4300A`); a Subaccount (`S`) leaf reuses its parent's already-resolved `insertionChildren`; any other level drills into real children, surfacing all of them as candidates when there is more than one (never guessing a single answer among real siblings).
  - **Structural identity and posting prefix are deliberately separate.** Selecting `4300A` preserves that exact value as the structural parent identity shown by the selector and carried in `insertionChildren`. The locked posting prefix supplied to the code field is derived independently from its first four numeric characters, `4300`. A suffix such as `1000` therefore produces the valid final 8-digit account code `43001000`; the UI and backend must never concatenate `4300A` with the suffix or replace the structural parent identity with `4300`.
  - **Frontend:** `artifacts/chart-of-accounts/custom/NewAccountModal.jsx`'s `resolveInsertionCandidates()` mirrors the structural algorithm client-side over the already-loaded local tree node, so a single confident candidate becomes the modal's default parent, while zero or multiple candidates (genuine ambiguity — e.g. Account-level `430A` fanning out into several real Breakdown children) leave the parent selector empty for the user to pick manually rather than guessing. `derivePostingPrefix()` then converts the selected structural parent code to the separate numeric code prefix. `artifacts/chart-of-accounts/custom/AccountTreeView.jsx` also resolves the selected row against the tree's **unfiltered** `indexById` map (`currentRecordForModal`) before handing it to the modal, since an active text/type filter clones virtual folder nodes with a pruned `children` array that would otherwise starve this resolution of a node's real children. The Account-Type sibling-match in `deriveDefaultAccountType()` likewise now prefers each candidate leaf's own `insertionChildren[0].value` over `parentCode4` when matching siblings under a resolved structural parent.
  - **`parentCode4`/`parentCode4Name` are now a fallback only** — used solely when a caller hands a bare `{searchKey, summaryLevel}` shape with no `elementLevel`/`children` at all (an older API response, a legacy tree shape, or a minimal test fixture predating this field). Even in that fallback path, the numeric-only heuristic (`summaryLevel === 'Y' && code.length === 4 && /^\d+$/.test(code)`) no longer matches a letter-suffixed 4-character code, so the original bug stays closed even without structural data.
- **Full-dataset self-fetch (bug fix):** `ListView.jsx` only ever hands the tree one paginated page of leaves (`hook.items`, capped at `BATCH_SIZE`) and does not forward `hasMore`/`loadMore` to the `Table` it renders — a scroll-triggered "load more" also can't work for a collapsed-by-default tree, and a tree needs the complete leaf list upfront just to know which root headings exist. This previously made entire top-level roots (e.g. `PYG`, `O`) silently disappear whenever no leaf under them landed in the first page — confirmed live on a tenant with 659 leaf accounts across 4 roots, where only 2 roots ever rendered. `AccountTreeView` now fetches its own complete leaf dataset directly (`GET {apiBaseUrl}/elementValue?_startRow=0&_endRow=9999`, same pattern already used by `NewAccountModal`'s parent selector) on mount and again after every new sub-account is saved, and renders that instead of the paginated `data` prop once it resolves. A small spinner + label appears in the toolbar while the fetch is in flight; on failure it falls back to the `data` prop and shows an error toast (`accountTreeFetchError`) instead of crashing or silently truncating the tree. When `apiBaseUrl` is not supplied (e.g. direct unit tests that only pass `data`), the component skips the self-fetch and renders `data` unchanged.
- **Initial loading skeleton, no partial-tree flash (ETP-5387):** the self-fetch above used to leave a visible gap — while the very first fetch was in flight, the component rendered the tree built from the partial `data` prop (only the roots that happened to have a leaf in ListView's first page) underneath the toolbar's "Cargando…" spinner, then replaced it with the full tree once the fetch resolved. That partial-tree-then-full-tree swap was itself a visible flash. The component now tracks `hasLoadedOnce` (whether any self-fetch attempt has ever *settled*, success or failure — set once in the effect's `finally`, never reset) and derives `showInitialSkeleton = !!apiBaseUrl && !hasLoadedOnce && isFetchingFull`. While that flag is true, `treeBody` renders a full-page `AccountTreeSkeleton` instead of any data-driven tree, so the partial `data` prop is never painted. The flag flips to `false` for good the moment the first attempt settles — including on failure, where the existing fallback-to-`data` + error-toast behavior takes over unchanged — so every later background refetch (after saving a new sub-account, or an active-toggle flip) only shows the toolbar's small spinner and never re-shows the full-page skeleton, even if that first attempt had failed. Gating on `hasLoadedOnce` rather than `fetchedData === null` is deliberate: the latter would flip back to `true` on a post-failure retry (since a failed fetch never sets `fetchedData`), wiping the already-visible fallback tree and re-showing the skeleton for what should be a quiet background retry.
- **Tree-native filter (bug fix):** The generic "Filtros" advanced-filter builder is designed to trigger backend-paginated re-fetches, which doesn't fit this component — it already self-fetches and holds the complete dataset in memory, so a backend-filtered response never actually reaches the rendered tree. `AccountTreeView` marks its columns `filterable: false` (so the generic builder's field picker no longer lists `searchKey`/`name`/`elementLevel`/`accountType`/`active` as choosable fields — the Filtros button itself remains, it simply has nothing tree-specific left to filter by) and instead owns a small filter row (code/name text search + account-type + active selects) that filters the already-materialized tree in JS via `filterTree`. A text match on a virtual folder's displayed code or name keeps that folder and its relevant descendant leaves; account-type filtering is still applied to those leaves, so unrelated leaf types are removed. This supports letter-suffixed folder codes such as `430A`, auto-expands matching ancestors, and hides branches with no matching descendant.

## Gap assessment
- A chart-of-accounts screen often carries stricter accounting semantics such as deleting accounts with activity or account-type-specific behavior. Those rules are not visible in the current contract or generated UI, so they remain gaps or open ambiguities.
- The presence of a `Parent Account` field suggests hierarchical setup, but the current evidence does not show how hierarchy depth, rollups, or parent eligibility are enforced.
- The custom subaccount-creation modal (`NewAccountModal.jsx`) renders `Account Type` as a required dropdown sourced from `ACCOUNT_TYPE_UI_KEYS`, defaulting from the selected parent/sibling account's existing type (falling back to Expense (`E`), the AD column default, when no parent/sibling type can be resolved).
- The balance columns show useful review data, but no evidence here explains whether they are point-in-time totals, ledger-derived live balances, period-sensitive balances, or mock/demo placeholders outside real backend data.

## Manual verification
1. Open `/chart-of-accounts` from the Finance menu and confirm the list loads through the generated window route.
2. Confirm the table shows only SearchKey, Name, Element Level, and Account Type — Debit, Credit, and Balance are no longer rendered as visible columns.
3. Search by Code, Name, and Account Type.
4. Create an account and confirm the editable fields are limited to Code, Name, Account Type, and optional Parent Account.
5. Open an existing account at `/chart-of-accounts/:recordId` and confirm the detail view matches the same maintenance scope.
6. Verify that debit, credit, and balance are review-only values and are not editable in the form.
7. Check whether the UI allows changing `isActive`; it is now a live toggle both on the tree row and in the generated form (see steps 18-19 below for the tree-row toggle flow).
8. If hierarchical accounting behavior is expected, try assigning a parent account and confirm whether any validation or restrictions actually exist.
9. Open an existing `xxxx0000` subaccount such as `10000000` or `10100000` and confirm the editable setup fields render as read-only, and that the tree row shows a lock icon.
10. Try to create a new `xxxx0000` subaccount and confirm the backend rejects it.
11. Expand a leaf account's full folder path (e.g. `A` → `A.A` → `A.A.I` → `200` → `2000`) and confirm each level renders as its own nested, expandable folder matching Etendo Classic's "Combinación de cuentas" grouped view — not a flat 4-digit group.
12. Confirm a real subaccount not ending in `0000` (e.g. `20000001`) shows no lock icon and remains fully editable.
13. Confirm the tree loads fully collapsed on first visit. Expand a folder path down to a specific account, navigate to another window, then return to Chart of Accounts and confirm the same folders are still expanded (and everything else still collapsed).
14. On a tenant with accounts under all 4 root headings (Asset, Liability, Revenue, Memo/Other), confirm all 4 roots appear in the tree — not just the ones with leaves in the first paginated batch.
15. Type a code or name fragment into the tree's filter box and confirm matching leaves and virtual folders (including a letter-suffixed folder such as `430A`) remain visible with their relevant descendants, while unrelated branches are hidden and matching ancestors are auto-expanded.
16. With a virtual-folder text match active, select an account type and confirm the matching folder remains but only descendant leaves of that account type remain visible.
17. Clear the filter and confirm the tree returns to whatever manual expand/collapse state was set before filtering — not the auto-expanded filter state.
18. Open "New Sub-account" from a selected leaf row and confirm the parent-account field is a searchable combobox, and confirm Account Type pre-fills to match the parent/sibling account's type.
19. Toggle an existing subaccount's Active switch off, confirm the tree PATCHes and the row updates; toggle it back on.
20. Confirm the Active toggle is disabled (not just visually locked) for a protected `0000`-suffixed placeholder leaf.
21. Open "New Sub-account" for a parent prefix that already has children (e.g. `2000`) and confirm the suffix field's placeholder shows the next available 4-digit suffix under that prefix (highest existing + 1), not the generic `0000`. Pick a prefix with no children yet and confirm it falls back to `0000`.
22. Try to create a subaccount whose full 8-digit code already exists and confirm the toast reads "Account `<code>` already exists" (localized), not the generic "Could not create sub-account" message.
23. Open `/chart-of-accounts` and confirm no small count badge appears next to the "Chart of Accounts" title — other list windows (e.g. General Ledger Configuration) still show theirs, confirming this is a per-window opt-out, not a global change.
24. Locate Account-level (`C`) heading `430A`, whose real children `4300A`, `4304A`, and `4309A` are Breakdown-level (`D`) nodes. Select `430A` and open "New Sub-account": confirm the parent selector is left unselected because several real children fan out, then choose `4300A` and confirm the selector retains that structural identity while the code field locks the numeric prefix to `4300`. Enter suffix `1000` and confirm submission sends the valid final code `43001000`. Repeat with the tree's text/type filter active to confirm filtering does not change the resolved candidates.
25. On desktop with a long account tree, scroll through the tree and confirm the toolbar actions and text/type filters remain visible in the single opaque sticky controls wrapper anchored to the existing `ScrollPane`; confirm the tree remains the only scrolling surface and no nested scrollbar appears.
26. Repeat the long-tree scroll check on a narrow mobile viewport, including after the toolbar wraps, and confirm all actions and filters remain contained in the same sticky wrapper, readable, and free of a second nested overflow area.
27. On a tenant with a large chart of accounts, open Finanzas > Plan de cuentas and confirm no visible partial-tree flash (e.g. only 2 of 4 roots) before the full tree appears — a loading skeleton (no data) should show instead, then swap to the complete tree in one shot. Throttle the network to make this easier to observe. Save a new sub-account and confirm the tree does NOT skeleton-flash again on that background refetch — only the toolbar spinner briefly appears. Force the first fetch to fail (e.g. block the request), confirm the fallback-to-partial-`data` + error toast still works, then trigger a retry (save or active-toggle) and confirm the skeleton does not come back on that retry either.

## Automated evidence
- `tools/app-shell/src/menu.json` exposes `chart-of-accounts` in the Finance menu.
- `tools/app-shell/src/windows/registry.js` maps `chart-of-accounts` to the generated window loader.
- `artifacts/chart-of-accounts/generated/web/chart-of-accounts/index.jsx` implements a generated single-entity list/detail flow for `account`.
- `artifacts/chart-of-accounts/generated/web/chart-of-accounts/AccountForm.jsx` shows the editable setup fields and the `isActive` checkbox, now editable rather than read-only.
- `artifacts/chart-of-accounts/generated/web/chart-of-accounts/AccountTable.jsx` shows the list columns and supported filters, including read-only financial review columns for debit, credit, and balance.
- `artifacts/chart-of-accounts/contract.json` defines one `account` entity, no child entities, GET/POST/PUT/DELETE endpoints, supported filters for `code`, `name`, and `accountType`, and a test manifest covering field presence, field types, searchable filters, frontend visibility, and backend-only system fields.
- `tools/app-shell/src/windows/registry.js` loads `@generated/chart-of-accounts/generated/web/chart-of-accounts/index.jsx`; its generated `ElementValuePage.jsx` imports `../../../custom/AccountTreeView` and `../../../custom/NewAccountModal`, so the live runtime sources are `artifacts/chart-of-accounts/custom/AccountTreeView.jsx` and `artifacts/chart-of-accounts/custom/NewAccountModal.jsx`. The similarly named files under `tools/app-shell/src/windows/custom/chart-of-accounts/` are not in this runtime import chain.
- `artifacts/chart-of-accounts/custom/AccountTreeView.jsx` — `buildGroupedTree()` builds the full N-level nested folder tree from each leaf's `ancestors` array (falling back to the legacy 2-level `parentCode4` grouping when `ancestors` is absent), and `AccountTreeRow` renders the lock icon for protected `0000`-suffixed leaves.
- `modules/com.etendoerp.go/src/com/etendoerp/go/schemaforge/handlers/ChartOfAccountsHandler.java` — `loadTreeData`/`buildAncestorChain` walk `AD_TreeNode` for the client's `"<ClientName> Element Value"` tree and inject `ancestors` + `elementLevel` per leaf in `applyHierarchyMetadata`.
- `tools/app-shell/src/windows/custom/chart-of-accounts/__tests__/AccountTreeView.vitest.jsx` hosts the Vitest suite but imports the runtime artifact component through `@generated/chart-of-accounts/custom/AccountTreeView.jsx` (`@generated` resolves to `artifacts/` in `tools/app-shell/vitest.config.js`). It covers both the legacy 2-level grouping and the full ancestor-chain nested tree, the `0000`-suffix lock-icon rule (locked, not locked, and virtual-folder-never-locked cases), expand/collapse persistence across unmount/remount (including a corrupt-`localStorage` fallback), and the full-dataset self-fetch (fetch fired with the correct URL/headers, all 4 roots rendered from the fetched data even when the paginated `data` prop only carries 2, self-fetch skipped when `apiBaseUrl` is absent, graceful fallback + error toast on fetch failure, and refetch-after-save). The same suite's sticky-controls regression asserts that toolbar actions and both filters are contained by the `sticky top-0 z-20 bg-card` wrapper and that no descendant adds `overflow`, `overscroll`, or `max-h` utilities that would create a nested scrolling container. **Initial-skeleton coverage (ETP-5387):** the skeleton (`account-tree-skeleton`) shows instead of any data-driven tree while the first self-fetch is in flight — even when the paginated `data` prop already has rows — and is replaced by the full tree in one shot with no intermediate partial-tree frame; a background refetch after saving a new sub-account does not re-show it; and, the key regression case, when the FIRST fetch fails and a LATER refetch is then triggered, the skeleton still does not come back — the fallback `data` tree stays visible throughout, only the toolbar spinner appears on the retry (this is what would fail under a naive `fetchedData === null` gate instead of `hasLoadedOnce`). Most of the suite's other 48-ish cases now render with `apiBaseUrl` absent by default (only the self-fetch and active/inactive-toggle blocks pass it explicitly), which also means they no longer unknowingly exercise the self-fetch path at all.
- `modules/com.etendoerp.go/src-test/src/com/etendoerp/go/schemaforge/handlers/ChartOfAccountsHandlerTest.java` covers `buildAncestorChain` (root node, node-exclusion, six-level PGC example, circular-reference cap, and JSON-null fallback for missing values).
- The hosted `AccountTreeView.vitest.jsx` suite additionally covers the expand-all recursion fix (every nested level opens, not just the first two), the tree-native filter (filtering by code/name/account-type/active with ancestor auto-expand, virtual-folder matching that retains relevant descendants including the `430A` regression, account-type filtering on leaves, and reverting to the manual expand/collapse state on clear), and the active/inactive toggle (checked/unchecked rendering, the PATCH request and its rollback-on-failure, the toggle being disabled for protected `0000`-suffixed leaves, and never rendering on virtual folder rows).
- `tools/app-shell/src/windows/custom/chart-of-accounts/__tests__/NewAccountModal.vitest.jsx` is likewise a test host: it imports `artifacts/chart-of-accounts/custom/NewAccountModal.jsx` through the `@generated` alias and renders the real `AccountCodeField` rather than a permissive input stub. It covers the searchable parent-account selector, the Account Type default, and the complete `430A` regression flow: search for and select structural parent `4300A`, verify the displayed numeric prefix is `4300`, enter suffix `1000`, submit, and assert the POST body contains `searchKey: "43001000"`. This proves selection and submission behavior, not search visibility alone.
- `artifacts/chart-of-accounts/custom/NewAccountModal.jsx`'s `resolveInsertionCandidates()`/`deriveDefaultParentId()` and `artifacts/chart-of-accounts/custom/AccountTreeView.jsx`'s `currentRecordForModal` unfiltered-index lookup implement the ETP-5399 `ElementLevel`-based resolution described above. The hosted Vitest suites import these artifact modules directly and cover a letter-suffixed `Breakdown` node resolving to itself, a `Subaccount` leaf reusing its own `insertionChildren`, single-real-child recursion, multi-child ambiguity leaving the parent unselected, and the numeric-only legacy `parentCode4` fallback no longer matching a letter-suffixed 4-character code. The focused `AccountTreeView.vitest.jsx` regression test also verifies that searching displayed folder code `430A` makes the virtual folder and its relevant leaf searchable/visible while unrelated branches stay hidden. The scoped suite passes **101/101 tests** (65 `AccountTreeView` and 36 `NewAccountModal` tests).

## Theme roles

The window's live artifact custom components use the shared semantic theme.
Structural surfaces and controls consume background, card, foreground, muted, and
border roles; operational feedback uses success, warning, information, neutral,
and destructive roles. No local palette is used, so the active application theme
controls the appearance. The tree's column-header row, row hover/selection state,
and toolbar buttons (Expand/Collapse/"+ New Sub-account") now reuse the same
`components/ui/table.jsx`/`components/ui/button.jsx` tokens as every other
window's grid, instead of a bespoke selection color and uppercase header
treatment.
