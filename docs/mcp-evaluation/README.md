# MCP Evaluation

Everything about measuring and improving the **Etendo GO MCP server** as an agent-facing product:
the benchmark against a reference vendor MCP (Holded), the IMP-* improvement backlog, the MARI
readiness index, and one working file per improvement.

Refresh the measurement with the `/mcp-comparison` skill. **Never** write a status anywhere except
the registry.

## Layout

| Path | Role |
|---|---|
| [`mcp-improvements-registry.md`](mcp-improvements-registry.md) | **The registry — single source of truth.** Every IMP-* item with status, priority, class, repo, points, cohort and evidence pointer · **MARI** (§2.1–2.3) · M5 diagnostics (§2.4) · probe surfaces (§2.5) · **ACE** context-cost index (§2.6) · changelog (§4). The only file where a status may change |
| [`mcp-comparison-holded-vs-etendo-go.md`](mcp-comparison-holded-vs-etendo-go.md) | **The baseline benchmark.** Architecture contrast, tool/spec inventories, coverage matrix, and each item's `BEFORE`/`AFTER`/`Done when:` specification. Reference material |
| `mcp-comparison-post-audit-<date>.md` | **One run report per execution.** Live evidence rows, defects, new proposals, preference verdict, M1–M4, and the delta against the registry. **Must end with a closing snapshot** — current MARI broken into its four components, plus every registered item grouped Resolved / Pending P1 / Pending P2 with a one-sentence description each, so the report reads standalone. Read-only restatement of the registry; canonical example is the 2026-08-10 report §10 |
| [`imps/`](imps/) | **One working file per improvement.** Written while the item is being worked, not before |

### Run reports

| Date | Report | Headline |
|---|---|---|
| 2026-08-05 | [`mcp-comparison-post-audit-2026-08-05.md`](mcp-comparison-post-audit-2026-08-05.md) | Baseline. Registered IMP-11…IMP-15. **MARI 28** |
| 2026-08-06 | [`mcp-comparison-post-audit-2026-08-06.md`](mcp-comparison-post-audit-2026-08-06.md) | Full-coverage run: all 6 probe surfaces closed, M1/M2 re-measured on the frozen suite, IMP-16…IMP-21 registered, quota re-based 73 → 97. **MARI 28 → 49** |
| 2026-08-10 | [`mcp-comparison-post-audit-2026-08-10.md`](mcp-comparison-post-audit-2026-08-10.md) | IMP-15 wave verification, write-probe mode. First run where the number moved because the *product* moved: M2 40 % → 80 %, M1 2.1× → 1.4×. IMP-15 / IMP-10 / IMP-25 resolved; IMP-11 / IMP-12 / IMP-14 / IMP-16 advanced; IMP-22…IMP-25 registered, consuming the last of the quota (97/97). Two new P1s: **IMP-23** (`neo_batch` not atomic) and **IMP-24** (silent date corruption on write). Flags that the frozen suite now flatters the product (§6). **MARI 49 → 73 — KR met** |

## What the `imps/` files are for

A registry row is one line: status, points, an evidence pointer. That is the right size for a
scoreboard and the wrong size for actually fixing something. The run reports, in turn, record what
was *observed* — not what the code turned out to be.

An `imps/IMP-n.md` file holds the third thing: **the investigation**. Where the responsible code
actually lives, what the DB actually contains, which of the competing hypotheses survived contact
with the data, and what the fix therefore has to touch. It is written **while the item is worked**,
so it is empty for every item nobody has opened yet — that is expected, not a gap.

The distinction that matters: a run report may say *"0 of 157 fields carry `visibility`"*. That is an
observation, and it is compatible with several very different root causes. The IMP file is where
those causes get discriminated with evidence, and where a wrong first guess gets recorded as wrong
rather than quietly replaced.

**Conventions:**

- Naming: `imps/IMP-<n>.md`, zero-padding not used (`IMP-11.md`).
- Numbers are permanent. Never renumber, never recycle — the registry, the run reports and the base
  report all cross-reference them.
- The file may contradict an earlier diagnosis. When it does, say so explicitly and keep the
  superseded claim visible with the evidence that killed it. Silent overwrites are how the old
  five-places-status problem started.
- **Status still lives only in the registry.** An IMP file describes the work; it never declares the
  item resolved.

| Item | File | State of investigation |
|---|---|---|
| IMP-5 | [`imps/IMP-5.md`](imps/IMP-5.md) | Opened late in the item's life, so §1 exists only to give the five clauses a home and say where each was settled (IMP-15, IMP-17, or here); §3 onward is the 2026-08-12 work on clauses **(i)** and **(iv)**. §3 is the reason they are one change and not two: both are the same mistake in two places — a failure body assembled by whichever path caught the failure, instead of by the shape the tool description promises. **(iv)** is the *fourth* error funnel, and §4.2 is worth reading for why IMP-17 could not have seen it: `validateReportRequest` already uses the canonical envelope but runs **before** the handler, so the argument-validation half was enveloped and everything the handler raised after it escaped — IMP-17's closure was complete for what it enumerated and the enumeration was short by one. Normalizing inside `neoResponseToMcpResult` covers **nine call sites** at once (§4.3). Two decisions are deliberate refusals: `NeoResponse.error`'s nested shape is **not** changed, because `error.message` is what the React UI reads and the two surfaces are allowed to differ (§4.4, IMP-17 §4.4's reasoning); and no `seeAlso` is added, because the only two topics would send the agent to a recipe that cannot help for *"no accounting schema with currency is configured"* — pinned by a test, since a deliberate omission and a forgotten key look identical in a response (§4.6). §4.5's normalization is additive and idempotent for a specific reason: rule 1 returns an already-canonical body untouched, which is what protects IMP-17's and IMP-24's richer envelopes, and a nested object's other keys are **lifted rather than discarded** because a handler that reported a `field` meant the agent to see it. §4.7 records a finding **not** acted on — the Aging handler answers 422 for an instance-level configuration failure no agent can correct, the case IMP-17 §4.1 resolved the other way — because the handler authors its own status and guessing for it is over-reach. **(i)** is one condition with two shapes (§5.1): a failure inside `executeBatch` returns `committed:false`, while the FK-by-name pre-pass returned the resolver's flat error with `failedAt` bolted on and **no `committed` key**, so an agent following the tool description read `false` from a missing key by luck. §5.2 notes that `ToolRegistry`'s description never lied — it already promised the full shape, so the fix makes an existing promise true, the cheaper direction whenever it is available. §5.3 turns IMP-23 §1's accident into a claim: `atomic:true`/`persisted:[]` are true **by construction** because the pre-pass runs before the transaction opens, with a hint that says so instead of reusing BatchService's "rolled back as a unit". §5.5 shares the outcome keys as `public` constants (`REF_PREFIX` precedent) since two spellings of `committed` drifting apart *is* this clause's failure. **Both clauses verified live** post-deploy (§6): (iv) returns the flat `{status:422, error:"validation_error", detail:…}` with `seeAlso` absent in the real response and not only in the test, and (i)'s C9 re-run returns every key the `neo_batch` description promises, `failedAt.id` included, with the resolver's `detail`/`field` preserved inside the nested `error`. Two things in §6 are worth more than the pass/fail. **§6.1**: the two clauses converge on **different** shapes — `error` stays an object in (i) and becomes a code string in (iv) — and that is not a contradiction but a consequence of which key each surface makes branchable (`committed` for a batch, `error` itself for a handler); one global shape would have broken one of the two contracts. **§6.2** is a deliberate non-measurement: the two-op case (a valid op preceding the failure) was **not** probed, because the pre-pass has always run before the transaction opened (IMP-23 §1), so this clause changed the *reporting* and never the persistence — probing it would measure a property the fix did not touch while risking a real order, so the atomicity claim rests on call order (§5.3) and says so. **§7 closes clause (iii)** the same day, and it is the section to read if you only read one: the clause's own wording turned out to be wrong about the write verbs. "Read-verb wrapped, write-verb bare" holds for the *error* bodies, but on the *success* bodies all four DAL-backed verbs forward core's `{"response":{data,status:0,startRow,…}}` untouched — `neo_delete` is bare only because it discards core's response and builds its own `{deleted,id}` — so flattening the reads alone would have made `neo_create`/`neo_update` the new outliers and left a follow-up that reads as a regression of this one. §7.1 draws the generalisable lesson: a clause records what was observed once, and what it *implies* about untested neighbours has to be re-checked. §7.2 is the smaller, sharper find — `buildNotFoundError`, this item's own flagship C17 envelope, was itself returned **wrapped**, so on one `neo_get` an unknown filter came back flat and a missing id came back nested; IMP-17 §8.6 measured "read errors are flat" on the DAL vector and never re-probed the not-found one, which is how the asymmetry survived inside the item that introduced the envelope. Now pinned by a test asserting the wrapper's *absence*, since key assertions cannot see a wrapper come back. §7.3: the flatten runs **last**, after `filterGetResponse` and `applyProjection`, so IMP-2 and IMP-18 are untouched code, and it lifts **by rule rather than allow-list** — which is why IMP-18's `unknownFields` reaches the top level without being named and why the next stage to annotate a response needs no edit here. §7.4: DAL's `status:0` is **dropped with nothing substituted**, because `status` on every other MCP body is an HTTP code (an agent branching on it read `0` where it expected `200`) and the absence of `error` is already the success discriminator — a second one is a second thing to drift, which is this item's failure mode. §7.5 leaves `neo_widget`'s `{response:{data,count}}` alone on §4.4's boundary (a handler's published contract, shared with the React dashboard, not core's DAL envelope) and notes no tool description changed: the `fields` description already promised `unknownFields` "alongside `data`", which the flatten makes literally true — §5.2's cheaper direction a second time. **§7.6 verifies (iii) live across all five CRUD verbs** — `neo_list` with a bad projection name comes back flat with the pagination keys intact and `unknownFields` at the **top level** (lift-by-rule working end to end, not only in the unit test), `neo_get`'s missing id returns the §7.2 envelope flat, and an authorized write probe (created from `neo_defaults`, updated, deleted in the same run, no pre-existing record touched) confirms create/update flat alongside `neo_delete`'s `{deleted,id}`. §7.7 records what the probe made visible and deliberately did **not** fix: the flattened write bodies are ~70 columns wide, equally wide inside the wrapper but now in plain sight — that is IMP-20's evidence confirmed, not folded in. Unverified: only §6.2's deliberate non-measurement |
| IMP-7 | [`imps/IMP-7.md`](imps/IMP-7.md) | The registry's one-line evidence cell bundles two defects of very different cost. **Half A** — a default resolved to `""` reported in `confirm` as if settled (`partnerAddress` on `sales-invoice/header`, with `metadata.unresolvedFields` empty) — root-caused to `apply()` classifying by key and never looking at the value, and to `NeoDefaultsService` populating `unresolvedFields` only from `catch` blocks. Fixed by routing blank `confirm` members into `unresolvedFields`, with the blank predicate shared with IMP-12 so the two views cannot drift. **Half B** — 7 localization compliance keys surviving `view:"minimal"` — is *not* a quick win: they are `editable` and structurally indistinguishable from a legitimate `confirm` member, so separating them needs a module-ownership criterion (a design decision, with three options weighed). Recommends splitting B out so A can be credited. Registry row stays ⚠️ until a `/mcp-comparison` run probes it |
| IMP-11 | [`imps/IMP-11.md`](imps/IMP-11.md) | Writer fixed (core `0c3f13d2b`) and backfill verified live on `etendo-go-local`; registry row stays ⏳ until the M2 measurement + staging re-verification |
| IMP-12 | [`imps/IMP-12.md`](imps/IMP-12.md) | Implemented and probed live twice (§9–§11): −89.1 % on `sales-invoice/header`, and the "omitting `view` changes nothing" half proved by `diff`. The specified filter rule was measured **wrong** (drags in 3 `readOnly` fields) and corrected to `editable ∧ mandatory ∧ no-default`. The probe then found `required` over-reporting 4 of 6 — `AD_Column.DefaultValue` is an incomplete proxy — so the view now cross-checks against `neo_defaults` (§12) — whose first attempt probed as a no-op because it read the wrong level of the response body (§13.1), re-fixed and confirmed live at 2 / 22 on two windows (§14). `fields:[…]` + `unknownFields` verified ✅ (§13.2). **6 of 7 done-when ✅ on local**; the last is a release. §14.3's divergence resolved by decision (option 3 — document the full dump's `userRequired` as a static approximation; no new IMP). §15 records a defect of the projection itself: on an uncurated entity `view:"create"` returns empty **and** its `hint` tells the agent not to look further — 89 of 230 MCP-exposed POST-able entities are in that state, 85 of them auxiliary sub-tabs that should lose `ispost` rather than be curated. Registry row stays ⏳ until a `/mcp-comparison` run re-measures M1/M2 |
| IMP-16 | [`imps/IMP-16.md`](imps/IMP-16.md) | Root-caused and **implemented** (§6) — not compiled, not deployed, verification owed (§7). The registry cell understates it three ways: there are **three** formats not two, `dd-MM-yyyy` is the *baseline* (not the outlier), and the write path does not "misparse" — it succeeds at producing **year 0012**. `@#Date@` never reads a session value: core `Utility.getContext:410` special-cases it to `DateTimeData.today`, whose generated `.xsql` hardcodes `"dd-MM-yyyy"` — so `NeoDefaultsService:719`'s ISO `#Date` override is dead code. The ISO fields are purely ETP-4244's callout return-path normalizer, which is why `sales-order` (no date callouts) is uniformly `dd-MM-yyyy` while the invoice windows are mixed. Silent corruption traced to lenient `JsonUtils.createDateFormat()`, with **neither** NEO coercer (`NeoTypeCoercionHelper`, `McpToolRouterSupport`) carrying a date branch — and it fires even when the agent sends no date, because both write paths re-run `injectMandatoryDefaults`. Fully explains B13's unrelated-field error. Seven hypotheses recorded as killed, including the two-`#Date` theory that held for most of the investigation. Fixed by ISO-out + tolerant-in through one shared `NeoDateFormat` helper applied at three points (defaults response + both write coercers), with unrecognized shapes passed through verbatim and the 422 deliberately phased to a later change; the lenient core parser is left to be raised upstream rather than patched here |
| IMP-17 | [`imps/IMP-17.md`](imps/IMP-17.md) | **Implemented and verified live 2026-08-11 / 08-12** (`3db3b4c5`, `08182a77`, `930e484f`) — registry row ✅ with the score still 0 / 3, the same status/score split IMP-23 used. **Read §8.1 before anything else in this file**: seven of eight probes passed, the eighth returned the 500 it was meant to remove, and the cause was a clause IMP-23 §9.4 had already written down — core HTML-escapes error messages, so the column arrives as `&quot;c_bpartner_location_id&quot;` and a pattern expecting a bare `"` matched nothing, leaving the field unresolvable and the violation falling through. The dump *was* stripped, which is what localised it. §9.4 mentioned the escaping in the same sentence as the dump; it was read as cosmetic noise and the loud half got fixed. The generalisable lesson: when a finding quotes a payload, every oddity in it is a fact about the input, and "ugly but harmless" has to be earned. Two further corrections came out of the live run — §4.3's premise that "a spec exposes a handful of entities" is **false** (`product` exposes 25), though the decision survives on a different argument (300 chars beats a `neo_discover` round trip); and **IMP-5 clause (iii) is settled** (§8.6): read *errors* are flat, read *successes* are still `{"response":{…}}`, so that nesting is the success body and never was this funnel. Starts by correcting its own citation: the cell cited B6, which is IMP-12's token-limit failure, while the 2026-08-06 run report registers the item from B13 + **B20** — the case the cell itself quotes. The two symptoms turned out to have **three** funnels and no fourth, which is what makes this one change rather than a sweep: `checkJsonServiceError` was declared to return a `String`, so a callout's Spanish prose *was* the entire response body and its validation branch shipped core's transport object (`status:-4`) verbatim; `route`'s single catch-all flattened every resolution failure into one line, including B20's `Entity not found: header` thrown **while the router held the list of names that would have worked**; and `NeoCrudHandler.checkJsonServiceResponse` called anything not a duplicate key a 500. **Absorbs IMP-23 §9.4** on the human's instruction — §9.4 named IMP-17 as its home, and §5.1's reasoning applies (a new number forces a quota re-base, ~3 MARI points for identical code) — turning the omitted-FK 500 + ~90-column failing-row dump into the `MISSING_REQUIRED_FIELDS` 400 naming the property. Three things are worth reading. **§4.1**: status follows the failure, not the verb, with one deliberate exception — core's `status:-1` is a 422 on a write and a **500** on a read, because there is nothing submitted to correct there and inviting retry-with-corrections is a loop with no exit. **§4.5**: closing C14 was not a freebie but a *regression the fix itself created* — the unknown-filter `IllegalArgumentException` was previously ugly prose that at least named the valid states, and the new catch-all would have downgraded it to `server_error`, i.e. "stop retrying" for a call one corrected word fixes; the general hazard is that a typed catch-all silently reclassifies every exception that used to be invisible. **§4.3**: `available` is carried for an unknown entity and deliberately **not** for an unknown spec — dozens of specs dumped into every mistyped call is an ACE cost the agent did not ask for, so the hint points at `neo_discover` instead, and a test pins the omission (`assertFalse(envelope.has("available"))`) rather than leaving it to a comment. Two over-reaches are refused in the code's own comments: no forced-English translation of callout messages (they come from `AD_Message` in the session locale, and pinning it is a separate change with a wider blast radius) and no invented `field`, since a callout rejects a *combination* far more often than one input — B13's is exactly that. §4.4 keeps ETP-3894's REST 400 shape because the React UI highlights fields from it, translating to the agent's 422 in `toMcpBatchFailure`, which that method's javadoc already says is its job. §4.6 is honest about a limit: detection keys on SQLState 23502 so the status is always right, but the column name comes from an English regex, so a translated `lc_messages` loses the field name — and the row-dump stripper keys on the *shape* of the leak (a parenthesised run ≥200 chars) for the same reason. Eight tests broke on a signature change and were **rewritten, not deleted**, with a nullable-`Tab` overload reproducing each old case exactly; two others had been passing only because their own stub threw the prose the assertion looked for. Unverified: nothing probed live |
| IMP-18 | [`imps/IMP-18.md`](imps/IMP-18.md) | **Implemented** 2026-08-10 (§5), then compiled, deployed and probed the same day; the registry row stays ⚠️ until a `/mcp-comparison` run re-scores it. The item was registered as a missing warning; it is really a consistency defect, since `unknownFields` already shipped on `neo_schema` under IMP-12. Two design points are load-bearing and both reject the cheap implementation: validation runs against the spec's **emittable keys** (post-`javaQualifier` rename), not the returned rows — otherwise the check goes silent on an empty result set, which is exactly when a typo makes an agent conclude the data is missing; and `view:"summary"` is deliberately never judged, since an unknown name there would be our bug. Fixing it exposed a second silent drop of the same kind in the supposedly-good path: `fields:["<fk>$_identifier"]` returned a row of nothing but `id`. **Verified live** the same day (§7, six read-only probes): C15 no longer reproduces, and the two design points above are now measured rather than argued — the typo is reported on an empty result set, and `fields:["dateAcct"]` is reported even though that property exists in the DAL, because the spec serves it as `accountingDate` |
| IMP-19 | [`imps/IMP-19.md`](imps/IMP-19.md) | **Implemented 2026-08-11 (`7282112e`) and verified live 2026-08-12** (§6) — registry row ✅ with the score still 0 / 3, the status/score split IMP-17 and IMP-23 both used. **Read §6.1 and §6.3 first.** §6.1 is the one that generalises: the session held the **pre-deploy** tool schemas, and that turned IMP-23 §9.3's stale-catalog limit from an obstacle into the right experimental condition — every probe was answered by the server's own validation rather than by a schema the client had already been given, which is the half that actually protects an agent. §6.2 measures §4's refusal correct: `generate_aging_receivable({})` **passes** validation, so declaring the phantom `recOrPay: required` would have rejected calls that work, and Tax's `meta` echoes 8 of 12 declared parameters filled from the code's own defaults — the typed contract being used, not merely published. `format:"pdf"` is now a 422 naming `supportedFormats:["json"]`; `generate_bank_statements({})` is refused `callable:false` at the router even while a stale client still lists it. Catalog retirement itself is confirmed **in code** (`ToolRegistry:143`) because an unauthenticated `tools/list` returns 401 and this session cannot mint a token — owed on a reconnected client. §6.3 is the find: the Aging call passed validation and then failed *inside the handler* with the nested pre-IMP-5 `{"error":{message,status}}` shape, no branchable code, no `seeAlso`. Report handlers are a **fourth** error funnel — `validateReportRequest` envelopes the argument half but runs *before* the handler, so IMP-17's three-funnel closure was complete for what it enumerated and this was never in it. Routed to **IMP-5 clause (iv)** rather than a new number. Worth reading for the two things the registry cell got wrong. First, the untyped `parameters` was **not a configuration gap**: `buildProcessParamSchema` emits a property only for a field with an `AD_Column`, and a report's inputs (`dateFrom`, `recOrPay`, `glId`) are arguments to a query, not columns of any table — there is nowhere in `ETGO_SF_*` for them to live, so no backfill could ever have filled the schema in, which is what puts the fix on the handler rather than in the data. Second, and larger than the other three clauses together, **5 of the 8 tools could never succeed**: callability was "any entity declares a `Java_Qualifier`", but a qualifier says a handler serves the entity, not that it generates a report, and five of them are React UI handlers dispatching on an `action` **query** param while `handleReport` POSTs a body with none — 405 or 400, always, from tools the catalog advertised as callable. §1 argues all four defects are one missing fact, which is why they get one fix: `NeoHandler.reportParameters()`/`reportFormats()` resolved once into a `NeoReportContract` that the tool schema, discover, the router's validation and the GET descriptor all read, so the contract an agent is shown and the one it is judged against cannot diverge. The distinction the whole of the hiding rests on is §2.1's `Optional.empty()` ("not a report generator" → **no tool emitted**; the five leave the catalog untouched and keep serving React over NEO REST, their only caller) versus `Optional.of(List.of())` ("a real report with no inputs" — B5's case, where an agent could not tell those two apart). §3 answers the `format` clause from the code instead of memory — three handlers returning `NeoResponse.ok(json)`, zero mentions of pdf/xlsx/csv, Jasper prohibited by ETP-4255, the one `application/pdf` path merely downloading an attachment, and the router never reading the string `"format"` — so `["json"]` is a finding, and `format:"pdf"` moves from silently ignored to a 422. §4 is a **refused** fix: the Aging descriptor called `recOrPay` required while the code defaults it, so honouring the descriptor would have rejected working calls; the descriptor is now rendered from the declaration instead, because that contract lived in three places, no two agreed, and `glId`/`showDetails` were read by the code and named in none. §5 notes the obsolete test that had to be **inverted**: it asserted callability from a qualifier alone, i.e. the exact belief that shipped the five dead tools. Still unverified after §6: `generate_inventory_stock_report`, Aging end to end (blocked by a missing accounting schema with currency on the probed org — instance data, not code), the live `tools/list` retirement, and the React UI still driving the five retired handlers over NEO REST |
| IMP-21 | [`imps/IMP-21.md`](imps/IMP-21.md) | **Resolved + verified live 2026-08-12** (§6) — the catalog now reports `actionCount:22` / `invokableCount:4`, no label left beginning `EM_`, `required` absent on all 22, and `businessCritical:true` on `documentAction`, `posted` and `aPRMProcessinvoice`, i.e. **3 of 22**, so the derivation discriminates rather than flagging everything. The probe also surfaced an **eighth defect the design does not cover** (§5 viii): `processNow` shares `processId` `111` with `documentAction` and is callable, yet carries no `actionValues`, no `actionParameter`, no `agentPrompt` and `businessCritical:false` — of the four actions now presented as callable it is the one that still misleads, and whether it should be curated out or reached by the derivation is a product answer rather than a widening of the rule. Score stays **0 / 3** until `/mcp-comparison` re-measures M4. Every measurement in the file is `sales-invoice/header`, and the fixes are generic and unconditional but no other catalog was read. Read §1 first: all three registered clauses hold in direction, two of the three counts are stale, and the third **understates the defect by a factor of eleven**. `businessCritical` is not merely uncurated on `documentAction` and `posted` — it is `N` on every button column in the instance (a `count(*)` over `ETGO_SF_FIELD` returns 0), so the flag has no producer for buttons at all and was emitted `false` 22 times out of 22. That is not a neutral default: `businessCritical:false` reads as "nobody needs to think before firing this". §1.1 names four defects the cell never had, and the first is larger than any registered one: **17 of 22 actions were curated `visibility:"discarded"` and still advertised `invokeVia:"neo_action"`**, so an agent was handed 22 entries all claiming to be callable with nothing separating the handful the window exposes from the ones curation had deliberately excluded — the truth *was* in each object, sitting next to a contradicting claim, in a field-visibility value designed for form fields. Also: one action advertised callable with no process behind it at all (`createLinesFrom`), a duplicate of `documentAction` with nothing saying which to use, and `agentPrompt` on exactly one of the 22. §2 tabulates every producer before any fix, and it is what makes the split defensible — the catalog was **not short of information**, it was carrying a second, unearned assertion beside the honest one (`required:true` next to `userRequired:false` on the same object, 10 times), so most of the fix is subtraction. §3 puts five of the seven defects in the generic Java layer, on the criterion `McpSchemaFieldBuilder`'s own `addActionValues` javadoc already sets: *which* `docAction` value is legal in a given state is per-window judgement and travels in `agentPrompt`, *that* a `docAction` button is consequential is structural. Three decisions are load-bearing. §3.1 removes `required` rather than setting it `false`, because a button has no payload slot so both values are wrong and the honest report is the absent key. §3.2 makes `invokeVia` **a claim rather than a decoration** — emitted only when the button is really invokable, otherwise `invokable:false` + a machine-readable `notInvokableReason` — while keeping the action **in** the catalog (knowing an action exists but is out of scope is useful; being told it is callable when it is not is the unrecoverable case) and treating an *uncurated* button as invokable, because absence of curation is not a decision to exclude and the opposite would silently retire actions on the 89 entities IMP-11 counts as uncurated. §3.3's `businessCritical` derivation is deliberately narrow — the `docAction` list binding and the `Posted` accounting trigger, both readable off the column and true on every document window — because a derivation that flagged everything would fail the same way `false`-on-everything did, in the other direction; curation always wins over it. §3.6 notes what the duplicate-action defect got **for free**: `aPRMProcessinvoice` is `discarded`, so one of the pair is now callable and the other says why it is not. `neo_action` is deliberately left ungated (§3.2): the registered defect is a description defect, and a runtime gate would change behaviour on a path the React UI also drives. §4.1 records three assertions **rewritten rather than deleted** — one of them, `buttonColumnWithNoProcessEmitsOnlyTrigger`, had been *pinning* defect (v). §5 keeps three things open rather than guessing, the largest being which of the 17 discarded actions should be promoted: §3.2 makes the current curation honestly reported, not right, and promoting one is a per-window product decision |
| IMP-22 | [`imps/IMP-22.md`](imps/IMP-22.md) | **Implemented 2026-08-11 (`c3ce6c5e`), live verification owed** — the registry row is **🔧**, worth zero, a mark added to the status vocabulary for this exact state. The read and write paths disagreed about the same string: `neo_selectors` returns `label:"Madrid, Avenida Independiente 23"` for `C_BPartner_Location_ID` given a `recordContext`, and sending that byte-identical label back as `partnerAddress` produced a 422 `not_found` — so an agent doing the correct thing got rejected for a value the server had just handed it. Root cause is not format (that is IMP-4, closed by IMP-15) but **scope**: all three `McpToolRouter` call sites (create `:448`, update `:585`, batch `:1117`) pass a literal `null` where the read path passes `args`, so the resolver ran with tab-derived context only, never the record's own fields — even though the parent was already in the same body. §3 revisits the reason the old javadoc gave for deferring the fix ("would require resolving fields in dependency order… a second, more invasive pass") and finds the premise avoidable: **order can be discovered by trying**, so no dependency graph is modelled. A cheap pass 0 does the id checks only — so a field already holding an id is context for free and never costs a selector call — then each pass retries the remainder, deferring failures. Termination is *no progress*, which is load-bearing because it is the **same** condition that makes the deferred error trustworthy: if nothing resolved this pass, no further context is coming, so the `not_found` is final rather than an artefact of missing context — the loop's exit criterion and the error's validity are one fact. §4 is the part worth reading: the naive version of this fix is **worse than the bug**, because copying an unresolved search string into `C_BPartner_ID` narrows the dependent selector to zero candidates and turns a resolvable field into a `not_found` pointing at the wrong field — so a sibling that is merely unresolved must look *absent*, not *wrong*, and both "still pending" and "failed unrecoverably" go into the exclusion list. The change is small only because `copySelectorContext` already mapped body keys onto classic selector params for the read path. 39 tests pass (7 new), and they assert **what context each selector call was given** rather than the end result — a green end-result test would pass against a resolver that guessed right for the wrong reason, which is how this defect survived. Unverified: the live C4/C5 re-run (the tests stub `NeoSelectorService`, so they say nothing about whether real AD data agrees), a three-deep dependency chain, and the O(n²) worst case, which is reasoned rather than measured |
| IMP-23 | [`imps/IMP-23.md`](imps/IMP-23.md) | **Both options implemented and verified live** — A on 2026-08-10, B on 2026-08-11 (§12): the batch now rolls back as a unit, so the row goes ✅ **while the score stays 0 / 5**, since status and score move on different evidence and the 5 / 5 is a `/mcp-comparison` measurement. **Investigated and half-implemented** first; the registry row moves ⏳ → ⚠️ and the score stays 0 / 5, because option A does not make the batch atomic. §1 reads the mechanism out of the code instead of inferring it: `BatchService` is **correct** — the unconditional `commitAndClose()` is four calls below it, in core's `DefaultJsonDataService#update`, so every op commits itself and the batch's own rollback finds an empty session. §2 checked the cheap fix before any code was written and **it does not exist** (no parameter on `add()`; `setDoRollback` is read only by `DalThreadHandler` at thread end; `TriggerHandler.disable()` makes `commitAndClose` *throw*), which is what makes real atomicity a core fork — option B, deferred **but kept under this same row rather than filed as a new IMP** (§5.1 corrects the file's own first recommendation: B is the first clause of IMP-23's title, so filing it separately would force a quota re-base and cost ~3 MARI points for identical code), so that a P1 agent-ergonomics fix does not wait on it and the shipped React invoice-scan ingest stays out of the blast radius. **Two findings the registry cell did not have:** the REST `POST /sws/neo/batch` shares `executeBatch` verbatim and has been non-atomic since it shipped, so this was never MCP-only; and the surviving ids were **already in memory** in `opResults` and `failureBody` threw them away — which is exactly why orphan `1000017` survived five days, the agent having been told `committed:false` and given no reason to look. Option A stops discarding them (`persisted`, `atomic:false`, `hint`), emitted even when empty so that “nothing survived” cannot be mistaken for “we are not saying”. Its most instructive part is the tests: they had to be **inverted**, not updated — `verify(obDal, never()).commitAndClose()` passed because the mocked CRUD seam meant nothing downstream ran, and `ToolRegistryTest` required the description to “mention atomic transaction”, which it did while being false. **Verified live the same day** (§9): the C10 re-run names the survivor and the recovery loop was executed, not asserted — `neo_get` confirmed the orphan, `neo_delete` removed it with the id the failure body supplied, and a sweep found nothing left. Both hint branches are confirmed, the empty one by accident and it is the better probe. §9.3 records a limit no code change reaches: the MCP client caches tool descriptions at session start, so an agent already connected keeps reading the retired atomicity promise — an argument for fixing the response body rather than the description alone. §9.4 notes a secondary finding left unregistered: the same tool returns a clean 400 for an over-length value but a 500 with a raw Postgres row dump for an omitted required FK. **Option B then followed the same day** (§11, commit `7159376c`): `NeoBatchJsonDataService` forks core's `update()` minus the two lines that end the transaction, a `ThreadLocal` in `BatchService` marks that a caller owns it, and **one line** at `NeoCrudHandler:237` feeds the whole create path — far cheaper than §4 estimated, because that injection point already fanned out by parameter. What is worth reading §11 for is not the fork but the three things that pushed back: making the subclass a CDI bean was **mandatory**, since core injects into it and `doPreAction` dereferences the injection, and it is only safe because Weld's static lookup matches the bean class *exactly*; the ownership flag had to live outside the subclass, because loading it runs core's Weld-dependent **static initializer**; and the failure body had to change **direction**, since after B a blanket `atomic:false` would be the mirror image of the original bug — so survivors are now detected generically, by Hibernate `Session` identity, which is also the only part of B a mocked test can see. The batch is atomic but **not hermetic** (`ProcessInvoiceUtil#process` commits by design — exactly what the new `atomic:false` reports), and the React invoice-scan ingest **silently gains atomicity** through the shared `executeBatch`. **§12 verifies B live, and is worth reading for how rather than what**: the C10 vector returns `atomic:true` / `persisted:[]` where it returned a survivor a day earlier — but *"nothing was persisted"* is the exact claim that was false before B, so the response was not believed. Two DB sweeps returned 0 rows **with a positive control**, because a zero from a filter that never matches looks identical to a zero from an empty result. Two things the probe does **not** settle are recorded rather than glossed: the process-commit branch (`atomic:false` + `persisted`) has only ever run against a mocked `Session` swap, and §9.3's stale-tool-description limit repeated verbatim — the description this session holds is two deploys old, now *accidentally* true while still omitting `persisted`. The score's 5 / 5 waits on a re-measure |
| IMP-24 | [`imps/IMP-24.md`](imps/IMP-24.md) | **Implemented, deployed and probed** 2026-08-10 (§4, §7); the registry row stays ⚠️ until a `/mcp-comparison` run re-scores it. The item shrank twice before any code was written (§1): the data-destroying vector was IMP-16's missing call site, and the post-deploy probe showed an unusable date is already refused — leaving *reshape an existing rejection*, a leaked DAL `status:-4` carrying a Java exception class name. Nearly all the work is in two gates rather than the error body (§2–§3), because either one missing would make the rejection wrong: `toCanonical` returns `null` for **two unrelated reasons**, so a blanket "`null` → 422" would have broken `2026-08-06T14:30:00+02:00`, which IMP-16 refuses *because the DAL parses it correctly*; and only a caller-supplied value may be rejected, since the server itself injects `dd-MM-yyyy` defaults and a 422 on a field the agent never sent is unactionable. Records plainly (§8) that IMP-16 §6.1's gate — logs showing the `WARN` never fires — is **not satisfied**: the log window holds no date write traffic at all, so the evidence is empty rather than clean, and the change ships on the different argument that the two gates exclude the at-risk population by construction — §7 then produced the first real data, pointing the right way (0 pass-through WARNs) without closing the gate, since the traffic is the fix author's own. **The live run also names two gaps instead of glossing them:** the offset classifier is covered by unit tests only, because the probe aimed at it hit a date-only property and never reached it, and the `neo_update` call site was blocked by the permission classifier. Its most useful probe is the negative one — a `dd-MM-yyyy` value is *repaired*, not rejected, so the change is a split between reparable and irreparable |
