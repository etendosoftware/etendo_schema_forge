{{#if header.companyLogoDataUrl}}
<img
  src="{{header.companyLogoDataUrl}}"
  alt="{{header.org_name}}"
  class="document-brand-logo"
  style="max-width: 42mm; max-height: 18mm; object-fit: contain; object-position: left center; display: block; margin-bottom: 2mm;"
>
{{/if}}
