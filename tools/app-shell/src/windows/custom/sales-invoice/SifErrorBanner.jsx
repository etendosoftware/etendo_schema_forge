// Re-exports the shared SifErrorBanner component so the quality-gate invariant
// (which requires a local file per window) can locate it without duplicating code.
export { default } from '../shared/SifErrorBanner';
