The `importedBankStatements` entity is a bank statement (FIN_BankStatement) of a financial account; `bankStatementLines` are its lines. Read both with neo_list/neo_get. Generic create/update/delete on both entities is refused with 405: change a statement with the `bank-statements` spec actions — `neo_action {spec:"bank-statements", entity:"bank-statements", id:<statement id>, action:"updateStatement"|"processStatement"|"reactivateStatement"|"deleteStatement"}` — and create one with `createStatement`/`importStatement` (id = the financial account). Catalogue: `neo_schema({spec:"bank-statements", view:"actions"})`.
