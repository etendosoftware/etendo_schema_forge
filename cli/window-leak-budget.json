{
  "_comment": "Ratchet for window/entity-specific literals leaking into GENERIC contract-ui components. Fails ONLY IF the count grows; the target is 0, reached gradually. Pay debt down by declarativizing leaks (see docs/reports/contract-ui-churn-analysis.md §8), then run `node cli/src/window-leak-budget.js --update`. Comments are ignored; string/JSX literals are scanned. Baseline 4 is L1 alone — the TAB_ICONS entries in DetailView.jsx ('custom:attachments', 'custom:sif', 'custom:pricing' and the bare 'products' key). DataTable.jsx scans at zero. Paid off so far: L2 (=== 'contact' ×1) in ETP-4708 via secondaryTabs.contact.excludeFields; L3 (field === 'product' ×4) in ETP-4708 via the priceTriggerField key on the lineConfig presets; L4 (internal-consumption-product ×1) by ETP-4525, which moved the registry to components/contract-ui/lookupDrawers.js. Clearing L1 is T10-T13 (declarative tab icons) and takes the target to 0. AN EARLIER BASELINE 7 -> 8 WAS A DETECTION IMPROVEMENT, NOT NEW DEBT: the fourth pattern (quoted window/entity name used as an object KEY) was added because the scanner could not see TAB_ICONS' `'products': WarehouseProductsIcon` entry — a bare key matches neither the `===` comparison patterns nor the `custom:` prefix. No code changed; one pre-existing leak simply became visible, so the true L1 count is 4, not 3. A ratchet that undercounts is worse than one with a high baseline, because it silently blesses the leaks it cannot see.",
  "paths": [
    "tools/app-shell/src/components/contract-ui/DetailView.jsx",
    "tools/app-shell/src/components/contract-ui/DataTable.jsx"
  ],
  "extensions": [
    ".js",
    ".jsx"
  ],
  "patterns": [
    "(===|!==)\\s*['\"](contact|product|order|invoice|sif|internalConsumption|internal-consumption|asset|amortization|attachments)\\b",
    "['\"]custom:[a-zA-Z]+['\"]",
    "['\"]internal-consumption[a-z-]*['\"]",
    "['\"](contact|products?|orders?|invoices?|sif|internalConsumption|assets?|amortization|attachments)['\"]\\s*:"
  ],
  "baseline": 4
}
